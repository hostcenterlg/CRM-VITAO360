---
phase: 03-timeline-mensal
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - scripts/phase03_timeline/03_validate_abc_timeline.py
  - data/output/phase03/abc_classification.json
  - data/output/phase03/validation_report.json
autonomous: true

must_haves:
  truths:
    - "Classificacao ABC correta para 537 clientes usando thresholds A>=2000, B>=500, C<500 sobre total acumulado 13 meses"
    - "Vendas mensais no DRAFT 1 cruzam 100% com sap_mercos_merged.json (zero divergencia)"
    - "CARTEIRA formulas INDEX/MATCH retornam valores corretos do DRAFT 1 (spot-check 10+ clientes)"
    - "TIME-01 PASS: vendas mes a mes preenchidas para 537 clientes (MAR/25-JAN/26)"
    - "TIME-02 PASS: dados cruzados entre Mercos e SAP via merged JSON"
    - "TIME-03 PASS: classificacao ABC recalculada com base na timeline completa"
  artifacts:
    - path: "scripts/phase03_timeline/03_validate_abc_timeline.py"
      provides: "Validation script for ABC + timeline cross-check"
    - path: "data/output/phase03/abc_classification.json"
      provides: "ABC distribution and per-client classification"
    - path: "data/output/phase03/validation_report.json"
      provides: "Full validation report with TIME-01..03 evaluation"
  key_links:
    - from: "DRAFT 1 vendas"
      to: "sap_mercos_merged.json"
      via: "CNPJ cross-check"
      pattern: "draft1_total == merged_total for each CNPJ"
    - from: "CARTEIRA formulas"
      to: "DRAFT 1 values"
      via: "INDEX/MATCH evaluation"
      pattern: "data_only=True shows DRAFT 1 values in CARTEIRA"
---

<objective>
Validar a populacao da Phase 3: cruzar vendas do DRAFT 1 contra sap_mercos_merged.json (zero divergencia), verificar formulas CARTEIRA via spot-check, confirmar classificacao ABC para 537 clientes, e avaliar formalmente os requisitos TIME-01, TIME-02, TIME-03.

Purpose: Garantir que os dados populados estao 100% corretos antes de prosseguir para Phase 4. A validacao cruzada elimina erros de mapeamento de colunas, CNPJ truncado, ou formulas quebradas.

Output: abc_classification.json com distribuicao ABC + validation_report.json com avaliacao TIME-01..03.
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-timeline-mensal/03-RESEARCH.md
@.planning/phases/03-timeline-mensal/03-01-SUMMARY.md
@data/output/phase02/sap_mercos_merged.json
@data/sources/crm/CRM_INTELIGENTE_VITAO360_V12_COM_DADOS.xlsx
</context>

<important_context>
## O que foi feito no Plan 03-01

- DRAFT 1 populado com 537 clientes (vendas MAR/25-JAN/26 + campos derivados)
- CARTEIRA expandida com 537 rows de formulas INDEX/MATCH
- V12 COM_DADOS salvo com ambas as abas atualizadas

## Validacoes Necessarias

1. **Cross-check DRAFT 1 vs merged JSON:** Para cada CNPJ, verificar que cada mes (MAR-DEZ + JAN/26) no DRAFT 1 bate exatamente com o valor no merged JSON. Tolerancia: 0.01 (float rounding).

2. **ABC recalculo independente:** Recalcular ABC a partir do merged JSON (total 13 meses: JAN/25-JAN/26) e comparar com o valor escrito no DRAFT 1 col 35. Devem ser 100% identicos.

3. **CARTEIRA formula spot-check:** Abrir V12 COM_DADOS com data_only=True (cached values) e verificar se CARTEIRA retorna os mesmos valores que DRAFT 1. NOTA: openpyxl data_only=True pode nao ter cached values se o arquivo nao foi aberto no Excel. Se cached values estiverem None, validar as formulas como STRING (confirmar que referenciam colunas corretas).

4. **Integridade geral:** Confirmar que V13 PROJECAO (19.224 formulas) nao foi tocado, que nenhuma outra aba do V12 foi corrompida.

## ABC Thresholds

A >= R$ 2.000, B >= R$ 500, C < R$ 500 (sobre TOTAL de todos os 13 meses: JAN/25 idx0 + FEV/25 idx1 + MAR/25-DEZ/25 idx2-11 + JAN/26)

## Python path
/c/Users/User/.pyenv/pyenv-win/pyenv-win/versions/3.12.10/python.exe
</important_context>

<tasks>

<task type="auto">
  <name>Task 1: Validar vendas DRAFT 1 vs merged JSON + ABC recalculo independente</name>
  <files>
    scripts/phase03_timeline/03_validate_abc_timeline.py
    data/output/phase03/abc_classification.json
    data/output/phase03/validation_report.json
  </files>
  <action>
Criar script Python que:

1. **Carrega sap_mercos_merged.json** (cnpj_to_vendas, jan26_vendas, stats).

2. **Abre V12 COM_DADOS com data_only=False** e le DRAFT 1:
   - Para cada row (4 ate max_row), ler CNPJ (col 2) normalizado
   - Para cada CNPJ, ler vendas mensais (cols 21-31) e campos derivados (cols 34-37, 43)

3. **Cross-check vendas mensal:**
   Para cada CNPJ presente em ambos (DRAFT 1 e merged JSON):
   - Comparar cada mes (MAR/25-DEZ/25): `abs(draft1_col - json_vendas[idx]) < 0.01`
   - Comparar JAN/26: `abs(draft1_col31 - jan26_vendas[cnpj]) < 0.01`
   - Registrar mismatches com detalhes (CNPJ, mes, valor_draft1, valor_json)

4. **Verificar cobertura:**
   - Todos os 537 CNPJs do merged JSON presentes no DRAFT 1?
   - Algum CNPJ no DRAFT 1 que NAO esta no merged JSON? (OK se for cliente pre-existente)
   - CNPJs duplicados no DRAFT 1?

5. **ABC recalculo independente:**
   Para cada CNPJ no merged JSON:
   - `total = sum(vendas[0:12]) + jan26_vendas.get(cnpj, 0)`
   - Classificar: A se total >= 2000, B se total >= 500, C se < 500
   - Comparar com valor lido do DRAFT 1 col 35
   - Registrar mismatches

6. **Campos derivados spot-check (10 clientes aleatorios):**
   Para 10 CNPJs aleatorios, verificar:
   - Nro COMPRAS (col 34) == count(meses > 0) no JSON
   - MESES POSITIVADO (col 36) == count(meses > 0) no JSON
   - TICKET MEDIO (col 37) == round(total / n_compras, 2) calculado
   - MEDIA MENSAL (col 43) == round(total / meses_pos, 2) calculado

7. **CARTEIRA formula validation:**
   Abrir CARTEIRA e verificar para 10 CNPJs:
   - Col 2 (CNPJ) presente como string 14 digitos
   - Cols 26-36 contem formulas (comecam com "=IFERROR(INDEX")
   - Col 25 contem formula "=SUM(Z"
   - Cols 38-42 contem formulas INDEX/MATCH ou calculo local
   Se data_only=True retorna cached values, comparar com DRAFT 1 valores.

8. **Integridade V13:**
   Abrir V13 PROJECAO e contar formulas na aba PROJECAO. Deve ser 19.224.

9. **Gerar abc_classification.json:**
   ```json
   {
     "distribution": {"A": N, "B": N, "C": N},
     "total_clients": 537,
     "thresholds": {"A": ">=2000", "B": ">=500", "C": "<500"},
     "by_client": [{"cnpj": "...", "abc": "A", "total": 12345.67, "meses_pos": 8}, ...],
     "comparison_with_draft1": {"match": N, "mismatch": N, "mismatches": []}
   }
   ```

10. **Gerar validation_report.json:**
    ```json
    {
      "phase": "03-timeline-mensal",
      "plan": "02",
      "date": "2026-02-17",
      "cross_check": {
        "total_cnpjs_json": 537,
        "total_cnpjs_draft1": N,
        "matched": N,
        "mismatches": [],
        "missing_from_draft1": [],
        "extra_in_draft1": []
      },
      "abc_validation": {
        "total_match": N,
        "total_mismatch": N,
        "distribution": {"A": N, "B": N, "C": N}
      },
      "derived_fields_spot_check": {
        "checked": 10,
        "all_pass": true/false,
        "failures": []
      },
      "carteira_formulas": {
        "checked": 10,
        "all_have_formulas": true/false,
        "issues": []
      },
      "v13_integrity": {
        "projecao_formulas": 19224,
        "status": "PASS"
      },
      "requirements": {
        "TIME-01": {"status": "PASS/FAIL", "detail": "537 clientes com vendas MAR/25-JAN/26 no DRAFT 1 + CARTEIRA"},
        "TIME-02": {"status": "PASS/FAIL", "detail": "Dados cruzados via sap_mercos_merged.json (SAP-First + Mercos complement)"},
        "TIME-03": {"status": "PASS/FAIL", "detail": "ABC recalculada: A=N, B=N, C=N (thresholds A>=2000, B>=500, C<500)"}
      },
      "overall": "PASS/FAIL"
    }
    ```

11. **Imprimir resumo no console** com status de cada verificacao e avaliacao TIME-01..03.

**Criterios de PASS por requisito:**
- TIME-01 PASS: 537 CNPJs com vendas preenchidas (MAR/25-JAN/26), zero mismatches vs merged JSON
- TIME-02 PASS: Dados originam do sap_mercos_merged.json que ja cruzou Mercos+SAP (verificar que 537 == stats.total_clientes)
- TIME-03 PASS: ABC recalculada para 537 clientes, 100% match entre calculo independente e DRAFT 1
  </action>
  <verify>
Rodar o script:
```bash
/c/Users/User/.pyenv/pyenv-win/pyenv-win/versions/3.12.10/python.exe scripts/phase03_timeline/03_validate_abc_timeline.py
```
Verificar:
- cross_check.mismatches == [] (zero divergencia)
- abc_validation.total_mismatch == 0
- derived_fields_spot_check.all_pass == true
- carteira_formulas.all_have_formulas == true
- v13_integrity.status == "PASS"
- requirements.TIME-01.status == "PASS"
- requirements.TIME-02.status == "PASS"
- requirements.TIME-03.status == "PASS"
- overall == "PASS"
  </verify>
  <done>
Validacao completa: zero divergencia entre DRAFT 1 e merged JSON, ABC 100% consistente, formulas CARTEIRA corretas, V13 intacto. TIME-01, TIME-02, TIME-03 todos PASS. Phase 3 formalmente verificada.
  </done>
</task>

</tasks>

<verification>
1. validation_report.json mostra overall == "PASS"
2. abc_classification.json tem distribuicao ABC para 537 clientes
3. Zero mismatches em cross-check vendas
4. Zero mismatches em ABC recalculo
5. V13 PROJECAO com 19.224 formulas intactas
6. TIME-01, TIME-02, TIME-03 todos PASS
</verification>

<success_criteria>
- Todos os 3 requisitos (TIME-01, TIME-02, TIME-03) avaliados como PASS
- Zero divergencia entre DRAFT 1 e fonte de verdade (merged JSON)
- ABC consistente entre calculo independente e dados no DRAFT 1
- V13 PROJECAO nao corrompido
- Relatorios JSON gerados para audit trail
</success_criteria>

<output>
After completion, create `.planning/phases/03-timeline-mensal/03-02-SUMMARY.md`
</output>
