---
phase: 09-blueprint-v2
plan: 06
type: execute
wave: 5
depends_on: ["09-05"]
files_modified:
  - data/output/CRM_VITAO360_V13_PROJECAO.xlsx
  - scripts/phase09_blueprint_v2/06_agenda_tabs_validation.py
  - data/output/phase09/phase09_validation_report.json
autonomous: true

must_haves:
  truths:
    - "4 AGENDA tabs exist: AGENDA LARISSA, AGENDA DAIANE, AGENDA MANU, AGENDA JULIO"
    - "Each AGENDA filters CARTEIRA by consultant name + followup date"
    - "AGENDA shows 40-60 prioritized daily tasks per consultant sorted by SCORE rank"
    - "AGENDA has dropdown columns for RESULTADO with options from REGRAS"
    - "All 263 CARTEIRA columns have valid formulas (no #REF!, no _xlfn.LET)"
    - "19,224 PROJECAO formulas intact"
    - "Total V13 file has 12 tabs (5 original + REGRAS + DRAFT 1 + DRAFT 2 + CARTEIRA + 4 AGENDA)"
    - "All 4 BLUE requirements satisfied (BLUE-01 through BLUE-04)"
  artifacts:
    - path: "data/output/CRM_VITAO360_V13_PROJECAO.xlsx"
      provides: "Complete V13 with 12+ tabs including CARTEIRA + 4 AGENDA tabs"
    - path: "scripts/phase09_blueprint_v2/06_agenda_tabs_validation.py"
      provides: "AGENDA builder + comprehensive Phase 9 validation script"
    - path: "data/output/phase09/phase09_validation_report.json"
      provides: "Full validation report with BLUE-01..04 assessment"
  key_links:
    - from: "AGENDA tabs"
      to: "CARTEIRA (full row data)"
      via: "FILTER() + SORT() by consultant name + followup date"
      pattern: "FILTER.*CARTEIRA.*SORT"
    - from: "AGENDA RESULTADO dropdown"
      to: "REGRAS section 1 (RESULTADO list)"
      via: "Data validation dropdown referencing REGRAS!$A$5:$A$20"
      pattern: "Data.*Validation.*REGRAS"
    - from: "AGENDA filled results"
      to: "DRAFT 2 (daily paste by Leandro)"
      via: "Manual copy/paste in daily operational cycle"
      pattern: "operational_cycle"
---

<objective>
Create the 4 individual AGENDA tabs (LARISSA, DAIANE, MANU, JULIO) that pull prioritized daily tasks from CARTEIRA, and run comprehensive validation of the entire Phase 9 output.

Purpose: The AGENDA tabs are the final output that consultants actually USE daily. They transform the CARTEIRA intelligence into an actionable task list. This is the culmination of the REGRA PRINCIPAL: "Gerar AGENDA DIARIA INTELIGENTE por consultor (40-60 atendimentos priorizados por ranking automatico)". The validation ensures all 4 BLUE requirements are met and the entire CARTEIRA pipeline is functional.

Output: V13 with 12+ tabs, complete operational CRM ready for daily use. Validation report confirming BLUE-01 through BLUE-04.
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-blueprint-v2/09-RESEARCH.md
@.planning/phases/09-blueprint-v2/09-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 4 AGENDA tabs with FILTER/SORT formulas and dropdowns</name>
  <files>
    scripts/phase09_blueprint_v2/06_agenda_tabs_validation.py
    data/output/CRM_VITAO360_V13_PROJECAO.xlsx
  </files>
  <action>
Create a Python script that opens V13 and creates 4 AGENDA tabs.

**Tab names:** "AGENDA LARISSA", "AGENDA DAIANE", "AGENDA MANU", "AGENDA JULIO"

**Consultant name mapping** (from Phase 8 findings):
- LARISSA = "LARISSA" (matches PROJECAO col D, 224 clients)
- DAIANE = "DAIANE STAVICKI" (canonical name from Phase 4)
- MANU = both "HEMANUELE DITZEL (MANU)" and "MANU DITZEL" (Phase 8 alias discovery)
- JULIO = "JULIO GADRET" (canonical name from Phase 4)

**Each AGENDA tab structure:**

Row 1: Title "AGENDA {CONSULTANT_NAME}" + DATE filter cell
Row 2: Headers
Row 3+: Dynamic data from CARTEIRA

**Two approaches (choose based on Excel 365 target per Research):**

APPROACH A (FILTER + SORT -- preferred for Excel 365):

In cell A3, place a dynamic array formula:
=SORT(FILTER(CARTEIRA!{selected_columns}, (CARTEIRA!L$4:L$557="{consultant_name}")*(CARTEIRA!AS$4:AS$557<=A1)*(CARTEIRA!AS$4:AS$557<>""), CARTEIRA!{score_col}$4:$557), {score_column_index}, -1)

Where:
- {selected_columns} = the columns visible in AGENDA (CNPJ, NOME, DATA FOLLOWUP, ESTAGIO, ACAO FUTURA, anchor columns, RESULTADO input columns)
- consultant_name = the canonical name for this consultant
- AS = PROX FOLLOWUP column
- A1 = date filter (default: TODAY())
- Sort by SCORE descending (highest priority first)

For MANU: use OR condition for both name variants:
(CARTEIRA!L$4:L$557="HEMANUELE DITZEL (MANU)") + (CARTEIRA!L$4:L$557="MANU DITZEL")

APPROACH B (INDEX/MATCH with helper columns -- LibreOffice fallback):
Create helper columns that number rows matching the filter criteria, then INDEX to pull sorted results. This is more complex but works everywhere.

**Per Research recommendation: Use FILTER() + SORT() for Excel 365.**

**Columns visible in AGENDA (per user decision):**

Fixed columns (always visible):
1. DATA (PROX FOLLOWUP from CARTEIRA col AS)
2. NOME FANTASIA (from CARTEIRA col A)
3. CNPJ (from CARTEIRA col B)
4. SCORE / RANK (intelligence ranking)
5. ESTAGIO FUNIL (from CARTEIRA)
6. TEMPERATURA
7. SINALEIRO

Anchor columns (minimizable):
8. SITUACAO
9. DIAS SEM COMPRA
10. CURVA ABC
11. ULTIMO RESULTADO
12. ACAO FUTURA
13. DATA ULT ATENDIMENTO

Green input columns (consultant fills these):
14. RESULTADO (dropdown from REGRAS section 1)
15. MOTIVO (dropdown from REGRAS section 3)
16. OBSERVACAO (free text)

**Data Validation (dropdowns):**

For RESULTADO column: create data validation dropdown referencing REGRAS!$A$5:$A$20
For MOTIVO column: create data validation dropdown referencing REGRAS!$A$33:$A$55

```python
from openpyxl.worksheet.datavalidation import DataValidation

dv_resultado = DataValidation(
    type="list",
    formula1="REGRAS!$A$5:$A$20",
    allow_blank=True,
    showDropDown=False  # Note: in openpyxl, False=SHOW dropdown (counterintuitive)
)
dv_resultado.error = "Selecione um RESULTADO valido"
dv_resultado.errorTitle = "Resultado Invalido"
ws.add_data_validation(dv_resultado)
dv_resultado.add(f"N3:N1000")  # or the RESULTADO column range
```

**Green styling for input columns:**
- Light green fill (#E2EFDA or similar) for RESULTADO, MOTIVO, OBSERVACAO columns
- Unlocked cells (protection-ready for when Leandro shares with consultants)

**Freeze panes:** Freeze at D3 (fixed columns always visible while scrolling right through anchor columns)

**Tab color:** Different color per consultant tab for visual distinction.

Write for all 4 consultants.

Python: /c/Users/User/.pyenv/pyenv-win/pyenv-win/versions/3.12.10/python.exe
  </action>
  <verify>
Verify:
1. 4 AGENDA tabs exist with correct names
2. Each tab has FILTER or equivalent formula referencing CARTEIRA
3. Date filter cell (A1) defaults to TODAY() or a reasonable date
4. RESULTADO column has data validation dropdown
5. MOTIVO column has data validation dropdown
6. Green fill applied to input columns
7. freeze_panes set on each AGENDA tab
8. MANU tab handles both name variants
  </verify>
  <done>
4 AGENDA tabs created with FILTER/SORT formulas pulling from CARTEIRA. Dropdowns for RESULTADO and MOTIVO. Green input columns styled. Each consultant gets prioritized daily tasks sorted by intelligence SCORE. Operational cycle ready.
  </done>
</task>

<task type="auto">
  <name>Task 2: Comprehensive Phase 9 validation and BLUE-01..04 assessment</name>
  <files>
    data/output/phase09/phase09_validation_report.json
  </files>
  <action>
Run comprehensive validation of the entire Phase 9 output. Check every structural and functional requirement.

**Structural checks:**

1. Tab count: V13 must have 12+ tabs (PROJECAO, LOG, DASH, REDES_FRANQUIAS_v2, COMITE, REGRAS, DRAFT 1, DRAFT 2, CARTEIRA, AGENDA LARISSA, AGENDA DAIANE, AGENDA MANU, AGENDA JULIO)

2. CARTEIRA column count: exactly 263 (col JC exists, col JD does not)

3. CARTEIRA row count: 554 data rows (rows 4-557), 3 header rows

4. Column grouping: count columns with outline_level > 0, expect >= 200

5. Super-groups: verify 6 groups identifiable in row 1

6. Anchor columns: at least 6 columns with outline_level 0 or unset within grouped sections

7. freeze_panes: CARTEIRA = "AR6"

**Formula checks:**

8. CARTEIRA total formula count: >= 100,000

9. Zero #REF! errors: scan all formula cells for "#REF!" text (open with data_only=True, check for error values)

10. Zero _xlfn.LET: scan formulas for "_xlfn.LET" pattern -- must be 0

11. Bounded ranges: sample 20 random formulas, verify none use full-column refs ($A:$A)

12. MERCOS block: spot-check 5 rows, verify INDEX/MATCH formulas reference DRAFT 1

13. FUNIL block: spot-check 5 rows, verify array formulas reference DRAFT 2

14. FATURAMENTO: verify 12 months x 15 sub-columns = 180 data columns + 6 anchors = 186 total

15. JUSTIFICATIVA: verify COUNTIFS with DATE functions for weekly ranges

16. Intelligence: SCORE column has weighted formula, RANK exists

**Cross-tab validation:**

17. PROJECAO: still has 19,224 formulas intact

18. LOG: still has 20,830 rows

19. REGRAS: motor section has 63 combinations (rows 220-282)

20. DRAFT 1: >= 554 data rows with CNPJ in expected column

21. DRAFT 2: headers correct, historical data present

22. AGENDA tabs: each has FILTER/SORT formula referencing CARTEIRA

**Requirement assessment:**

BLUE-01: CARTEIRA tem 263 colunas organizadas em 6 grupos
- Check: 263 columns present, 6 super-groups in row 1
- Per locked decision: "Seguir 6 secoes do V12 (NAO 8 do roadmap)"

BLUE-02: Colunas fixas mantidas
- Check: columns A-B (NOME FANTASIA, CNPJ) always visible (outline_level 0)
- freeze_panes ensures left columns always visible

BLUE-03: Colunas originais preservadas e funcionais
- Check: MERCOS block structure matches V12 audit
- All original DRAFT 1 INDEX/MATCH patterns working

BLUE-04: Grupos organizados em 6 secoes V12
- Check: MERCOS, FUNIL, SAP, STATUS SAP, DADOS CADASTRAIS SAP, FATURAMENTO present
- 3-level grouping operational

Output phase09_validation_report.json with:
- 22+ checks with PASS/FAIL status
- BLUE-01..04 formal assessment
- Statistics: total formulas, total tabs, total rows
- Warnings or notes for any edge cases
  </action>
  <verify>
Check phase09_validation_report.json:
1. All 22+ checks have a status
2. Zero FAIL on critical checks (column count, formula count, #REF!, tab count)
3. BLUE-01 through BLUE-04 all assessed
4. Total formulas >= 100,000
5. PROJECAO formulas = 19,224
6. 12+ tabs present
  </verify>
  <done>
Comprehensive validation complete. Phase 9 validation report generated with all structural, formula, cross-tab, and requirement checks. BLUE-01 through BLUE-04 formally assessed. V13 CRM ready for Phase 10 final validation.
  </done>
</task>

</tasks>

<verification>
1. 4 AGENDA tabs functional with FILTER/SORT from CARTEIRA
2. Dropdowns for RESULTADO and MOTIVO from REGRAS
3. Green input columns styled for consultant use
4. 22+ validation checks pass
5. BLUE-01..04 all PASS
6. Total CARTEIRA formulas >= 100,000
7. PROJECAO 19,224 formulas intact
8. V13 has 12+ tabs, all functional
9. No #REF! errors anywhere
10. No _xlfn.LET functions anywhere
</verification>

<success_criteria>
- 4 AGENDA tabs generate daily prioritized task lists per consultant
- RESULTADO/MOTIVO dropdowns linked to REGRAS reference tables
- Comprehensive validation report confirms all BLUE requirements PASS
- V13 CRM operational: CARTEIRA intelligence + AGENDA workflow complete
- REGRA PRINCIPAL satisfied: "Gerar AGENDA DIARIA INTELIGENTE por consultor (40-60 atendimentos priorizados)"
</success_criteria>

<output>
After completion, create `.planning/phases/09-blueprint-v2/09-06-SUMMARY.md`
</output>
