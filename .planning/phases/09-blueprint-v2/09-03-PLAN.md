---
phase: 09-blueprint-v2
plan: 03
type: execute
wave: 3
depends_on: ["09-01", "09-02"]
files_modified:
  - data/output/CRM_VITAO360_V13_PROJECAO.xlsx
  - scripts/phase09_blueprint_v2/03_build_carteira_mercos_funil.py
autonomous: true

must_haves:
  truths:
    - "CARTEIRA tab exists in V13 with exactly 263 columns (A through JC)"
    - "3-level column grouping works: Level 1 (super-group), Level 2 (sub-group), Level 3 (detail)"
    - "6 anchor columns visible when groups collapsed (1 per super-group)"
    - "3 header rows: R1=super-group name, R2=sub-group name, R3=column name"
    - "MERCOS block (cols A-AQ) has working INDEX/MATCH formulas referencing DRAFT 1"
    - "FUNIL block (cols AR-BJ) has formulas referencing DRAFT 2 for most-recent-record lookup"
    - "554 data rows (rows 4-557) with formulas for every client in DRAFT 1"
  artifacts:
    - path: "data/output/CRM_VITAO360_V13_PROJECAO.xlsx"
      provides: "V13 with CARTEIRA tab (263 cols, MERCOS+FUNIL populated)"
    - path: "scripts/phase09_blueprint_v2/03_build_carteira_mercos_funil.py"
      provides: "Reproducible CARTEIRA skeleton + MERCOS/FUNIL builder"
  key_links:
    - from: "CARTEIRA col B (CNPJ)"
      to: "DRAFT 1 col B (CNPJ)"
      via: "INDEX/MATCH match key for all MERCOS formulas"
      pattern: "MATCH.*DRAFT 1.*\\$B"
    - from: "CARTEIRA col B (CNPJ)"
      to: "DRAFT 2 col D (CNPJ)"
      via: "CSE array formula match key for all FUNIL formulas"
      pattern: "DRAFT 2.*\\$D.*\\$B"
    - from: "CARTEIRA TEMPERATURA (FUNIL block)"
      to: "REGRAS motor rows 220-282"
      via: "Concatenated INDEX/MATCH (SITUACAO & RESULTADO)"
      pattern: "REGRAS!\\$G\\$220"
---

<objective>
Build the CARTEIRA tab skeleton with all 263 columns, 3-level grouping, anchors, and inject formulas for the MERCOS block (43 cols, behavioral data from DRAFT 1) and FUNIL block (19 cols, atendimento data from DRAFT 2).

Purpose: This is the core structural creation -- the CARTEIRA tab that will hold the 360-degree client view. MERCOS and FUNIL blocks are the most formula-intensive sections (INDEX/MATCH patterns, CSE array formulas, REGRAS motor lookups). Building them first validates the formula-injection pipeline before tackling the massive FATURAMENTO block.

Output: V13 with CARTEIRA tab containing 263 columns, grouped headers, and working formulas for MERCOS (A-AQ) and FUNIL (AR-BJ) blocks across 554 client rows.
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-blueprint-v2/09-RESEARCH.md
@.planning/phases/09-blueprint-v2/09-01-SUMMARY.md
@.planning/phases/09-blueprint-v2/09-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CARTEIRA tab skeleton with 263 columns and 3-level grouping</name>
  <files>
    scripts/phase09_blueprint_v2/03_build_carteira_mercos_funil.py
    data/output/CRM_VITAO360_V13_PROJECAO.xlsx
  </files>
  <action>
Create a Python script that opens V13 and creates the CARTEIRA tab with the complete 263-column structure.

**Load column specification** from data/output/phase09/carteira_column_spec.json (Plan 01 output). This JSON has all 263 column definitions with header text, outline levels, widths, hidden states, and super-group assignments.

**Create CARTEIRA tab:**

1. Set outline properties: summaryBelow=False, summaryRight=False (buttons LEFT of group for anchor visibility)

2. Write 3 header rows for ALL 263 columns:
   - Row 1: Super-group name (MERCOS, FUNIL, SAP, STATUS SAP, DADOS CADASTRAIS SAP, FATURAMENTO) -- only on anchor column of each super-group, merged or repeated per V12 pattern
   - Row 2: Sub-group name (IDENTIDADE, REDE, EQUIPE, STATUS, COMPRA, ECOMMERCE, VENDAS, RECORRENCIA, PIPELINE, PERFIL, MATURIDADE, CONVERSAO, ACAO, SINAL, etc.)
   - Row 3: Column name (NOME FANTASIA, CNPJ, RAZAO SOCIAL, etc.)

3. Set column widths from column_spec.json

4. Apply 3-level column grouping from column_spec.json:
   - Level 0: Anchor columns (always visible, no outline_level set)
   - Level 1: Super-group member columns
   - Level 2: Sub-group member columns
   - Level 3: Detail columns (FATURAMENTO sub-sub-cols)
   For each column: ws.column_dimensions[letter].outline_level = level (if > 0)
   Set hidden state per V12 pattern (collapsed by default for clean view)

5. Set freeze_panes at "AR6" (matching V12 -- FUNIL anchor column, below header rows + 2 info rows)

6. Set auto_filter on row 3: A3:JC{max_row} where max_row = 557 (3 header rows + 554 data rows)

7. Write CNPJ values in column B (rows 4-557) from DRAFT 1 col B. These are the match keys for all formulas.

8. Write NOME FANTASIA in column A (rows 4-557) from DRAFT 1 (static values, same as V12 pattern).

**Apply header styling** per PROJECT.md constraints:
- Theme LIGHT, Arial 10pt for headers, bold
- Super-group headers (R1): colored backgrounds matching V12 pattern (use column_spec.json colors if captured, else: MERCOS=green tones, FUNIL=blue tones, SAP=orange, FATURAMENTO=purple/gray)
- Column names (R3): standard formatting

Python: /c/Users/User/.pyenv/pyenv-win/pyenv-win/versions/3.12.10/python.exe
  </action>
  <verify>
Run the script and verify:
1. CARTEIRA tab exists with exactly 263 columns (cell JC3 has a value, cell JD3 is empty)
2. Row 1 has super-group names on anchor columns
3. Row 3 has column names on all 263 columns
4. Column B rows 4-557 have CNPJ values (554 clients)
5. freeze_panes = "AR6"
6. At least 200 columns have outline_level > 0 (210 from research)
7. 6 super-groups visible in row 1
  </verify>
  <done>
CARTEIRA skeleton complete: 263 columns, 3-level grouping, 6 super-groups, anchors, freeze panes, auto filter, 554 client CNPJs in column B. Ready for formula injection.
  </done>
</task>

<task type="auto">
  <name>Task 2: Inject MERCOS and FUNIL block formulas for 554 client rows</name>
  <files>
    data/output/CRM_VITAO360_V13_PROJECAO.xlsx
  </files>
  <action>
Continue in the same script (or separate execution step). Using the formula templates from v12_formula_audit.json and the column mapping from draft1_column_map.json, inject formulas for the MERCOS block (cols A-AQ, 43 cols) and FUNIL block (cols AR-BJ, 19 cols) for all 554 data rows (rows 4-557).

**MERCOS block formulas (cols A-AQ):**

Columns with static data (already written in Task 1): A=NOME FANTASIA, B=CNPJ

Columns with INDEX/MATCH from DRAFT 1 (use bounded ranges $B$3:$B$25000 instead of $B:$B per Pitfall #4):
- Use template: =IFERROR(INDEX('DRAFT 1'!${target_col}$3:${target_col}$25000,MATCH($B{row},'DRAFT 1'!$B$3:$B$25000,0)),"")
- Apply to: RAZAO SOCIAL (C), UF (D), CIDADE (E), EMAIL (F), TELEFONE (G), DATA CADASTRO (H), REDE REGIONAL (I), TIPO CLIENTE (J/K), ULT REGISTRO (K), CONSULTOR (L), VENDEDOR ULT PEDIDO (M), SITUACAO (N), PRIORIDADE (O), DIAS SEM COMPRA (P), DATA ULT PEDIDO (Q), VALOR ULT PEDIDO (R), CICLO MEDIO (S)
- E-COMMERCE sub-group (T-X): ACESSO B2B, ACESSOS PORTAL, ITENS CARRINHO, VALOR B2B, OPORTUNIDADE
- Map each column name to its V13 DRAFT 1 column using draft1_column_map.json

Columns with internal calculations:
- TOTAL PERIODO (Y): =SUM(Z{row}:AJ{row})
- Monthly sales (Z-AJ): INDEX/MATCH from DRAFT 1 monthly columns
- TIPO CLIENTE anchor (AK): INDEX/MATCH from DRAFT 1

RECORRENCIA sub-group (AL-AQ):
- N COMPRAS (AL): INDEX/MATCH from DRAFT 1
- CURVA ABC (AM): INDEX/MATCH from DRAFT 1
- MESES POSITIVADO (AN): INDEX/MATCH from DRAFT 1
- MEDIA MENSAL (AO): INDEX/MATCH from DRAFT 1
- TICKET MEDIO (AP): INDEX/MATCH from DRAFT 1
- MESES LISTA (AQ): INDEX/MATCH from DRAFT 1

**FUNIL block formulas (cols AR-BJ):**

These use the complex CSE array formula pattern referencing DRAFT 2 (most recent record by CNPJ). Use bounded ranges.

Template for "most recent record" lookup (adapt from V12, rewrite to avoid _xlfn.LET):
=IFERROR(INDEX('DRAFT 2'!${target_col}$3:${target_col}$25000,MATCH(1,INDEX(('DRAFT 2'!$D$3:$D$25000=$B{row})*('DRAFT 2'!$A$3:$A$25000=MAX(IF('DRAFT 2'!$D$3:$D$25000=$B{row},'DRAFT 2'!$A$3:$A$25000))),0,1),0)),"")

Apply to:
- ESTAGIO FUNIL (AR), PROX FOLLOWUP (AS), DATA ULT ATENDIMENTO (AT), ACAO FUTURA (AU), ULTIMO RESULTADO (AV), MOTIVO (AW)
- TIPO CLIENTE (AX), TENTATIVA (AY)
- FASE (AZ), ULTIMA RECOMPRA (BA)
- TEMPERATURA (BB): use REGRAS motor lookup:
  =IFERROR(INDEX(REGRAS!$G$220:$G$282,MATCH(N{row}&AV{row},REGRAS!$A$220:$A$282&REGRAS!$B$220:$B$282,0)),"")
- DIAS ATE CONVERSAO (BC), DATA 1o CONTATO (BD), DATA 1o ORCAMENTO (BE), DATA 1a VENDA (BF), TOTAL TENTATIVAS (BG)
- PROX ACAO (BH), ACAO DETALHADA (BI)
- SINALEIRO (BJ): Complex IF formula (rewrite from _xlfn.LET to nested IFs per Pitfall #3)

For SINALEIRO, use the nested IF rewrite from research:
=IF(P{r}="","",IF(OR(N{r}="PROSPECT",N{r}="LEAD"),"NOVO",IF(N{r}="NOVO","EM DESENVOLVIMENTO",IF(S{r}=0,IF(P{r}<=50,"VERDE",IF(P{r}<=90,"AMARELO","VERMELHO")),IF(P{r}<=S{r},"VERDE",IF(P{r}<=S{r}+30,"AMARELO","VERMELHO"))))))

**CRITICAL:** Use bounded ranges (e.g., $A$3:$A$25000) NOT full column references ($A:$A) per Pitfall #4.

After writing formulas, verify formula count for MERCOS+FUNIL: ~(43+19) * 554 = ~34,000 formulas approximately (minus static columns).
  </action>
  <verify>
Verify:
1. MERCOS block: spot-check 5 random rows -- col P (DIAS SEM COMPRA) has INDEX/MATCH formula referencing DRAFT 1
2. FUNIL block: spot-check 3 random rows -- col AR (ESTAGIO FUNIL) has DRAFT 2 array formula
3. TEMPERATURA (col BB): formula references REGRAS!$G$220:$G$282
4. SINALEIRO (col BJ): nested IF formula (no _xlfn.LET)
5. No full-column references ($A:$A) in any formula -- all bounded
6. Total formula count in MERCOS+FUNIL blocks: >= 25,000
7. PROJECAO 19,224 formulas still intact
  </verify>
  <done>
MERCOS block (43 cols) and FUNIL block (19 cols) fully populated with formulas for 554 rows. All formulas use bounded ranges. SINALEIRO rewritten without _xlfn.LET. TEMPERATURA references REGRAS motor. CARTEIRA has ~34,000+ formulas in first 62 columns.
  </done>
</task>

</tasks>

<verification>
1. CARTEIRA tab: 263 columns, 3 header rows, 554 data rows
2. 3-level grouping operational (outline levels 1-3 on 210+ columns)
3. 6 anchor columns visible (A, AR, BK, BN, BQ, BZ/CA per V12 pattern)
4. MERCOS formulas: INDEX/MATCH from DRAFT 1 with bounded ranges
5. FUNIL formulas: CSE array from DRAFT 2, REGRAS lookup for TEMPERATURA
6. SINALEIRO: nested IF (no _xlfn.LET)
7. freeze_panes = "AR6", auto_filter set
8. PROJECAO 19,224 formulas intact
</verification>

<success_criteria>
- 263-column CARTEIRA tab with proper grouping and anchors
- MERCOS block fully functional with INDEX/MATCH from DRAFT 1
- FUNIL block fully functional with DRAFT 2 lookups and REGRAS motor references
- No full-column references, no _xlfn.LET, no #REF! errors
- 554 rows of formulas for complete client roster
</success_criteria>

<output>
After completion, create `.planning/phases/09-blueprint-v2/09-03-SUMMARY.md`
</output>
