---
phase: 09-blueprint-v2
plan: 05
type: execute
wave: 4
depends_on: ["09-03", "09-04"]
files_modified:
  - data/output/CRM_VITAO360_V13_PROJECAO.xlsx
  - scripts/phase09_blueprint_v2/05_intelligence_engine.py
autonomous: true

must_haves:
  truths:
    - "Layer 1 (Ranking): Each client has a weighted SCORE from 6 factors (URGENCIA TEMPORAL 30%, VALOR CLIENTE 25%, FOLLOW-UP VENCIDO 20%, SINAL COMPRA 15%, TENTATIVA 5%, SITUACAO 5%)"
    - "Layer 2 (Pipeline vs Meta): Consultant-level meta diaria, coverage ratio, and pipeline gap calculated"
    - "Layer 3 (Urgency Alert): Conditional flag when pipeline does NOT cover remaining meta -- triggers PROSPECCAO EXTERNA alert"
    - "Retrofeeding cycle: RESULTADO triggers ACAO FUTURA + DATA PROXIMA ACAO + task description via REGRAS motor"
    - "PRIORIDADE column sorts clients by weighted score for agenda generation"
  artifacts:
    - path: "data/output/CRM_VITAO360_V13_PROJECAO.xlsx"
      provides: "V13 CARTEIRA with complete intelligence engine (3 layers)"
    - path: "scripts/phase09_blueprint_v2/05_intelligence_engine.py"
      provides: "Reproducible intelligence engine builder"
  key_links:
    - from: "CARTEIRA SCORE column"
      to: "REGRAS SCORE RANKING section (rows 209-216)"
      via: "Weighted formula using 6 factor weights from REGRAS"
      pattern: "REGRAS!.*209.*216"
    - from: "CARTEIRA PROX FOLLOWUP (col AS)"
      to: "DRAFT 2 results + REGRAS follow-up days"
      via: "DATE(last_result) + follow_up_days from REGRAS section 1"
      pattern: "REGRAS!.*follow"
    - from: "Layer 3 ALERT column"
      to: "Pipeline sum vs remaining META"
      via: "IF(pipeline_value < remaining_meta, ALERT, OK)"
      pattern: "ALERTA.*PROSPECCAO"
---

<objective>
Build the 3-layer intelligence engine that transforms the CARTEIRA from a data sheet into the decision-making tool that generates daily agenda priorities. This is the heart of the REGRA PRINCIPAL: without this intelligence, the CARTEIRA is just a database.

Purpose: Layer 1 ranks clients by who to contact first (weighted score). Layer 2 calculates whether the consultant's pipeline covers their meta. Layer 3 triggers urgency alerts when pipeline falls short. Together they generate the prioritized task list that becomes each consultant's daily AGENDA.

Output: V13 CARTEIRA with intelligence columns (SCORE, RANK, PIPELINE, COVERAGE, ALERT) + retrofeeding formulas (ACAO FUTURA, DATA PROXIMA ACAO) working via REGRAS motor. PRIORIDADE column ready for AGENDA tab filtering.
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-blueprint-v2/09-RESEARCH.md
@.planning/phases/09-blueprint-v2/09-01-SUMMARY.md
@.planning/phases/09-blueprint-v2/09-03-SUMMARY.md
@.planning/phases/09-blueprint-v2/09-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Layer 1 (Ranking Score) and retrofeeding formulas</name>
  <files>
    scripts/phase09_blueprint_v2/05_intelligence_engine.py
    data/output/CRM_VITAO360_V13_PROJECAO.xlsx
  </files>
  <action>
Create a Python script that opens V13 CARTEIRA and enhances existing formula columns + adds intelligence formulas.

**Layer 1: Ranking de Prioridade (who to contact first)**

The SCORE RANKING section in REGRAS (rows 209-216) defines 6 weighted factors. Build a composite SCORE formula in the PRIORIDADE column (col O, or a new column if O is already used -- check column_spec.json).

The 6 factors from research (REGRAS section 16):

1. URGENCIA TEMPORAL (30% weight):
   Score = MIN(100, (DIAS_SEM_COMPRA / MAX(CICLO_MEDIO, 1)) * 100)
   Formula: =MIN(100, P{row}/MAX(S{row},1)*100)
   Higher = more urgent (client overdue for purchase)

2. VALOR DO CLIENTE (25% weight):
   Score based on CURVA ABC + TIPO CLIENTE
   =IF(AM{row}="A",100,IF(AM{row}="B",60,IF(AM{row}="C",30,0))) -- ABC score
   + IF(AX{row}="Fidelizado",20,IF(AX{row}="Maduro",15,IF(AX{row}="Recorrente",10,0))) -- maturity bonus

3. FOLLOW-UP VENCIDO (20% weight):
   Score = IF(AS{row}<TODAY(), MIN(100, (TODAY()-AS{row})*5), 0)
   If PROX FOLLOWUP is past due, score increases with days overdue

4. SINAL DE COMPRA (15% weight):
   Score based on e-commerce activity + TEMPERATURA
   =IF(BB{row}="QUENTE",100,IF(BB{row}="MORNO",60,IF(BB{row}="FRIO",20,0)))
   + IF(T{row}="SIM",20,0) -- B2B access bonus

5. TENTATIVA (5% weight):
   =IF(AY{row}="T1",100,IF(AY{row}="T2",80,IF(AY{row}="T3",60,IF(AY{row}="T4",40,20))))
   Earlier attempts = higher priority

6. SITUACAO (5% weight):
   =IF(N{row}="EM RISCO",100,IF(N{row}="ATIVO",80,IF(N{row}="INAT.REC",70,IF(N{row}="PROSPECT",50,IF(N{row}="NOVO",40,IF(N{row}="LEAD",30,IF(N{row}="INAT.ANT",20,0)))))))

**Composite SCORE formula:**
Place in a column within or after the FUNIL block (per column_spec.json positioning). If the existing PRIORIDADE column (col O) is already used for a different purpose, add the SCORE as an additional column or repurpose per V12 pattern.

SCORE = (factor1 * 0.30) + (factor2 * 0.25) + (factor3 * 0.20) + (factor4 * 0.15) + (factor5 * 0.05) + (factor6 * 0.05)

Build this as a single composite formula or use helper sub-formulas. For readability, use helper columns if space allows, or build one large formula.

**RANK formula:**
=RANK(SCORE_col{row}, SCORE_col$4:SCORE_col$557)
This gives each client their priority position (1 = highest priority).

**Retrofeeding formulas (enhance existing FUNIL columns):**

The ACAO FUTURA (col AU) and PROX FOLLOWUP (col AS) columns already have formulas from Plan 03 that pull from DRAFT 2. Enhance them with REGRAS motor integration:

PROX FOLLOWUP (AS):
If DRAFT 2 has a result for this client, calculate: DATE_OF_LAST_RESULT + FOLLOW_UP_DAYS from REGRAS section 1.
=IFERROR(AT{row} + INDEX(REGRAS!$B$5:$B$20, MATCH(AV{row}, REGRAS!$A$5:$A$20, 0)), "")
Where AT = DATA ULT ATENDIMENTO, AV = ULTIMO RESULTADO, and REGRAS section 1 maps RESULTADO -> follow-up days.

ACAO FUTURA (AU):
Already pulls from DRAFT 2 in Plan 03. If empty, derive from REGRAS motor:
=IF(AU_draft2{row}<>"", AU_draft2{row}, IFERROR(INDEX(REGRAS!$F$220:$F$282, MATCH(N{row}&AV{row}, REGRAS!$A$220:$A$282&REGRAS!$B$220:$B$282, 0)), ""))

Write these formulas for all 554 data rows.

Python: /c/Users/User/.pyenv/pyenv-win/pyenv-win/versions/3.12.10/python.exe
  </action>
  <verify>
Verify:
1. SCORE column exists with composite weighted formula for all 554 rows
2. RANK column exists ranking clients 1-554
3. Spot-check: client with high DIAS SEM COMPRA + CURVA A should have high SCORE
4. Spot-check: client with recent followup + CURVA C should have lower SCORE
5. PROX FOLLOWUP enhanced with REGRAS follow-up days calculation
6. ACAO FUTURA has REGRAS motor fallback when DRAFT 2 is empty
7. No circular references
8. Weight sum = 100% (0.30 + 0.25 + 0.20 + 0.15 + 0.05 + 0.05 = 1.00)
  </verify>
  <done>
Layer 1 complete: weighted SCORE from 6 factors, RANK for priority ordering, retrofeeding formulas linking RESULTADO to ACAO FUTURA and PROX FOLLOWUP via REGRAS motor. Each client has a priority rank for agenda generation.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement Layers 2-3 (Pipeline vs Meta + Urgency Alerts) and conditional formatting</name>
  <files>
    data/output/CRM_VITAO360_V13_PROJECAO.xlsx
  </files>
  <action>
Continue enhancing the CARTEIRA with Layers 2 and 3. Per Claude's Discretion: implement as inline formulas in CARTEIRA columns (not auxiliary tab).

**Layer 2: Pipeline vs Meta (will the consultant hit their target?)**

Add 3-4 columns for pipeline analysis. Place them near the FUNIL block or after the intelligence columns (choose logical position based on column_spec.json available space).

META DIARIA (per consultant):
This needs a consultant-level calculation. Since CARTEIRA is per-client, calculate the consultant's remaining daily meta:
=IFERROR((INDEX('PROJECAO '!$L$4:$L$600,MATCH($B{row},'PROJECAO '!$B$4:$B$600,0))/12 - {current_month_realizado_col}{row}) / MAX(DAY(EOMONTH(TODAY(),0)) - DAY(TODAY()), 1), 0)

This gives: (META MES - REALIZADO MES) / remaining days in month = daily gap per client.

PIPELINE VALUE (clients in "momento ouro"):
=IF(OR(AR{row}="NEGOCIACAO",AR{row}="ORCAMENTO",AR{row}="CADASTRO",AR{row}="EM ATENDIMENTO"), AP{row}, 0)
Where AR = ESTAGIO FUNIL, AP = TICKET MEDIO
This flags clients actively in the pipeline with their expected value.

COVERAGE RATIO:
Need a consultant-level summary. Use SUMIFS for the consultant's pipeline:
=IFERROR(SUMIFS({pipeline_col}$4:$557, L$4:L$557, L{row}) / SUMIFS({meta_diaria_col}$4:$557, L$4:L$557, L{row}), 0)
Where L = CONSULTOR column.
Result: ratio of total pipeline / total daily meta for this consultant. >1 = on track, <1 = behind.

**Layer 3: Urgency Alert (Plan B needed?)**

ALERTA column:
=IF(AND({coverage_col}{row}<1, L{row}<>""), "ALERTA: PROSPECCAO EXTERNA", "")
If coverage ratio < 1 for this consultant's row, flag it.

More sophisticated version:
=IF({coverage_col}{row}>=1, "OK",
  IF({coverage_col}{row}>=0.7, "ATENCAO: pipeline apertado",
    IF({coverage_col}{row}>0, "ALERTA: pipeline insuficiente",
      "CRITICO: sem pipeline")))

GAP VALUE:
=IF({coverage_col}{row}<1, SUMIFS({meta_diaria_col}$4:$557, L$4:L$557, L{row}) - SUMIFS({pipeline_col}$4:$557, L$4:L$557, L{row}), 0)
Shows the R$ gap the consultant needs to fill.

**Conditional Formatting:**

Apply conditional formatting to key intelligence columns:

1. SCORE column: Color scale (red=low to green=high)
2. SINALEIRO (BJ): Already has IF formula, add CF:
   - "VERDE" -> green fill
   - "AMARELO" -> yellow fill
   - "VERMELHO" -> red fill
3. TEMPERATURA (BB):
   - "QUENTE" -> red/orange fill
   - "MORNO" -> yellow fill
   - "FRIO" -> blue/light fill
4. COVERAGE RATIO:
   - >= 1.0 -> green fill
   - 0.7-1.0 -> yellow fill
   - < 0.7 -> red fill
5. ALERTA column:
   - Contains "CRITICO" -> red fill + bold
   - Contains "ALERTA" -> orange fill
   - Contains "ATENCAO" -> yellow fill
   - "OK" -> green fill
6. FATURAMENTO % columns (%MES, %TRI, %YTD):
   - >= 100% -> green
   - 70-99% -> yellow
   - < 70% -> red

Apply CF rules ONCE (not per row -- use range-based rules). openpyxl CF applies to entire column ranges.

Write formulas for all 554 rows. Apply CF to data range (rows 4-557).
  </action>
  <verify>
Verify:
1. Layer 2 columns: META DIARIA, PIPELINE VALUE, COVERAGE RATIO have formulas for 554 rows
2. Layer 3: ALERTA column has conditional text for 554 rows
3. Spot-check: consultant LARISSA's COVERAGE should be calculable (SUMIFS over her clients)
4. Conditional formatting rules applied (count rules >= 6)
5. No circular references introduced
6. SINALEIRO, TEMPERATURA, % columns have appropriate CF
7. Total CARTEIRA formula count: should now be >= 105,000
8. PROJECAO 19,224 formulas intact
  </verify>
  <done>
3-layer intelligence engine complete. Layer 1: weighted SCORE ranking. Layer 2: pipeline vs meta with coverage ratio. Layer 3: urgency alerts with gap calculation. Conditional formatting applied to SCORE, SINALEIRO, TEMPERATURA, COVERAGE, ALERTA, and FATURAMENTO % columns. CARTEIRA is now an intelligent prioritization tool.
  </done>
</task>

</tasks>

<verification>
1. Layer 1: SCORE formula with 6 weighted factors, RANK column
2. Layer 2: META DIARIA, PIPELINE VALUE, COVERAGE RATIO per consultant
3. Layer 3: ALERTA column with 4-tier urgency classification
4. Retrofeeding: ACAO FUTURA + PROX FOLLOWUP linked to REGRAS motor
5. Conditional formatting: >= 6 rules applied to intelligence columns
6. No circular references
7. PROJECAO 19,224 formulas intact
8. File saves and opens without errors
</verification>

<success_criteria>
- 3-layer intelligence engine fully operational in CARTEIRA formulas
- Each client has a priority RANK based on 6 weighted factors
- Consultants can see pipeline coverage and urgency alerts
- Retrofeeding cycle: RESULTADO -> REGRAS -> ACAO FUTURA + DATA working
- Conditional formatting provides visual intelligence at a glance
</success_criteria>

<output>
After completion, create `.planning/phases/09-blueprint-v2/09-05-SUMMARY.md`
</output>
