---
phase: 09-blueprint-v2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/output/phase09/carteira_column_spec.json
  - data/output/phase09/v12_formula_audit.json
  - data/output/phase09/tab_name_map.json
  - scripts/phase09_blueprint_v2/01_audit_v12_carteira.py
autonomous: true

must_haves:
  truths:
    - "All 263 V12 CARTEIRA columns fully cataloged with name, super-group, sub-group, outline level, width, hidden state, and formula pattern"
    - "Every formula reference in V12 CARTEIRA mapped to its V13 equivalent tab/column"
    - "Tab name map defines V12->V13 substitutions for all formula references"
    - "Column specification JSON is machine-readable and drives Plans 03-06 without manual interpretation"
  artifacts:
    - path: "data/output/phase09/carteira_column_spec.json"
      provides: "Complete 263-column specification with grouping, formula patterns, header text"
      contains: "263 column entries"
    - path: "data/output/phase09/v12_formula_audit.json"
      provides: "Every distinct formula pattern extracted from V12 CARTEIRA rows 4-100"
      contains: "formula_patterns"
    - path: "data/output/phase09/tab_name_map.json"
      provides: "V12 tab name -> V13 tab name mapping for formula rewriting"
      contains: "DRAFT 1"
    - path: "scripts/phase09_blueprint_v2/01_audit_v12_carteira.py"
      provides: "Reproducible audit script"
  key_links:
    - from: "data/output/phase09/carteira_column_spec.json"
      to: "Plans 09-03, 09-04, 09-05"
      via: "Column positions and grouping levels drive skeleton builder and formula injectors"
      pattern: "outline_level.*super_group"
    - from: "data/output/phase09/v12_formula_audit.json"
      to: "Plans 09-03, 09-04, 09-05"
      via: "Formula templates are rewritten with tab_name_map and injected into V13"
      pattern: "formula_template"
---

<objective>
Deep audit of V12 COM_DADOS CARTEIRA tab: extract every column definition (name, group, sub-group, outline level, width, hidden state), classify every formula pattern used in data rows, and build the machine-readable specification that drives all subsequent CARTEIRA construction plans.

Purpose: The V12 CARTEIRA has 263 columns with complex 3-level grouping, anchors, and formulas referencing 4+ tabs. Without a precise audit, formula injection in Plans 03-05 will reference wrong columns/tabs. This is the "understand before building" step per user's locked decision.

Output: 3 JSON artifacts (column_spec, formula_audit, tab_name_map) that serve as the single source of truth for Plans 02-06.
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-blueprint-v2/09-RESEARCH.md
@.planning/phases/09-blueprint-v2/09-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract complete V12 CARTEIRA column specification and formula patterns</name>
  <files>
    scripts/phase09_blueprint_v2/01_audit_v12_carteira.py
    data/output/phase09/carteira_column_spec.json
    data/output/phase09/v12_formula_audit.json
    data/output/phase09/tab_name_map.json
  </files>
  <action>
Create a Python script that opens V12 COM_DADOS (data/sources/crm-versoes/v11-v12/CRM_INTELIGENTE_VITAO360_V12_COM_DADOS.xlsx) with openpyxl (data_only=False) and performs a comprehensive audit of the CARTEIRA tab (note: in V12 it may be named "COM_DADOS" or similar -- use find_sheet pattern from Phase 1 to locate it).

For ALL 263 columns (A through JC):
1. Read header rows 1-3 (R1=super-group, R2=sub-group, R3=column name)
2. Read column_dimensions: width, outline_level, hidden state
3. Sample formulas from rows 4, 10, 50, 100 (4 samples per column) to classify formula pattern
4. Detect formula references: which tabs are referenced (DRAFT 1, DRAFT 2, REGRAS, PROJECAO, internal)
5. Identify anchor columns (outline_level=0 or no level set within a grouped section)

Output carteira_column_spec.json with structure:
```json
{
  "total_columns": 263,
  "columns": [
    {
      "col_num": 1,
      "col_letter": "A",
      "header_r1": "MERCOS",
      "header_r2": "ANCORA",
      "header_r3": "NOME FANTASIA",
      "super_group": "MERCOS",
      "sub_group": "IDENTIDADE",
      "outline_level": 0,
      "width": 25,
      "hidden": false,
      "is_anchor": true,
      "formula_pattern": "static_data",
      "formula_template": null,
      "references_tabs": [],
      "sample_formulas": {"r4": "value", "r10": "value", "r50": "value", "r100": "value"}
    }
  ],
  "super_groups": [
    {"name": "MERCOS", "start_col": 1, "end_col": 43, "anchor_col": 1},
    {"name": "FUNIL", "start_col": 44, "end_col": 62, "anchor_col": 44}
  ],
  "outline_summary": {"level_0": N, "level_1": N, "level_2": N, "level_3": N}
}
```

Output v12_formula_audit.json with every DISTINCT formula pattern found, classified by type:
- "static_data": no formula, raw value
- "index_match_draft1": INDEX/MATCH referencing DRAFT 1
- "index_match_draft2_cse": complex CSE array formula referencing DRAFT 2 (most recent record pattern)
- "index_match_regras": concatenated MATCH referencing REGRAS motor
- "projecao_ref": formula referencing PROJECAO tab
- "internal_calc": formula referencing only CARTEIRA columns (SUM, IF, etc.)
- "countifs_draft2": COUNTIFS for weekly justificativas
- "let_function": formulas using _xlfn.LET (must be rewritten)

For each pattern, store the TEMPLATE with {row} placeholder for row number.

Output tab_name_map.json:
```json
{
  "v12_to_v13": {
    "DRAFT 1": "DRAFT 1",
    "DRAFT 2": "DRAFT 2",
    "REGRAS": "REGRAS",
    "PROJECAO ": "PROJECAO "
  },
  "notes": "Tab names confirmed via V13 sheet list. PROJECAO has trailing accent."
}
```

Also read V13 current sheet names to verify the tab_name_map is correct.

Python: /c/Users/User/.pyenv/pyenv-win/pyenv-win/versions/3.12.10/python.exe
  </action>
  <verify>
Run the script: /c/Users/User/.pyenv/pyenv-win/pyenv-win/versions/3.12.10/python.exe scripts/phase09_blueprint_v2/01_audit_v12_carteira.py

Verify:
1. carteira_column_spec.json has exactly 263 column entries
2. Every column has non-null header_r3 (column name)
3. super_groups array has 6 entries (MERCOS, FUNIL, SAP, STATUS SAP, DADOS CADASTRAIS SAP, FATURAMENTO)
4. outline_summary shows all 3 levels used
5. v12_formula_audit.json has at least 7 distinct pattern types
6. tab_name_map.json contains all 4 referenced tabs
7. No errors or warnings during execution
  </verify>
  <done>
263 columns fully cataloged. Formula patterns classified. Tab name map validated against V13 sheet names. All 3 JSON artifacts exist and are well-formed. Ready to drive Plans 02-06.
  </done>
</task>

<task type="auto">
  <name>Task 2: Validate audit completeness and generate V12-to-V13 column position map for DRAFT 1</name>
  <files>
    data/output/phase09/draft1_column_map.json
  </files>
  <action>
After the audit script runs, create a supplementary validation that:

1. Opens V12 DRAFT 1 tab and reads its column headers (row 3 or first header row)
2. Opens V13 DRAFT 1 (data/sources/drafts/) and reads its column headers
3. Builds a column position map: for each CARTEIRA formula that references DRAFT 1 by column letter, map V12 column -> V13 column

This is CRITICAL per Research Pitfall #7: V12 CARTEIRA formulas reference DRAFT 1 columns by letter (e.g., $L:$L = DIAS SEM COMPRA). V13 DRAFT 1 may have different column positions from Phase 3.

Output draft1_column_map.json:
```json
{
  "v12_draft1_columns": {"B": "CNPJ", "L": "DIAS SEM COMPRA", "O": "CICLO MEDIO", ...},
  "v13_draft1_columns": {"B": "CNPJ", ...},
  "remapping": {
    "DIAS SEM COMPRA": {"v12_col": "L", "v13_col": "?"},
    "CICLO MEDIO": {"v12_col": "O", "v13_col": "?"}
  },
  "v13_draft1_path": "data/sources/drafts/...",
  "v13_draft1_max_row": 554
}
```

Also validate:
- carteira_column_spec.json has all FATURAMENTO sub-columns (should be 186 in the FATURAMENTO super-group)
- Anchor columns are correctly identified (at least 1 per super-group)
- _xlfn.LET formulas are flagged for rewrite (Research Pitfall #3)
  </action>
  <verify>
Run the supplementary validation.

Check:
1. draft1_column_map.json has CNPJ in same position (col B) in both V12 and V13
2. All CARTEIRA formula references to DRAFT 1 have a valid V13 column mapping
3. FATURAMENTO super-group has exactly 186 columns in the column spec
4. At least 6 anchor columns identified (1 per super-group minimum)
5. Any _xlfn.LET formulas are documented in the audit
  </verify>
  <done>
V12-to-V13 DRAFT 1 column mapping complete. FATURAMENTO 186-column count verified. Anchor columns identified per super-group. All _xlfn.LET formulas flagged. Audit artifacts validated and ready for Plan 02 consumption.
  </done>
</task>

</tasks>

<verification>
1. All 4 JSON files exist in data/output/phase09/
2. carteira_column_spec.json: 263 entries, 6 super-groups, 3 outline levels
3. v12_formula_audit.json: at least 7 formula pattern types documented
4. tab_name_map.json: 4+ tab mappings with V13 validation
5. draft1_column_map.json: complete DRAFT 1 column remapping
6. No #REF!-prone mismatches between V12 formulas and V13 tab names
</verification>

<success_criteria>
- 263 columns fully specified with machine-readable JSON
- Every formula pattern has a reusable template with {row} placeholder
- Tab name map validated against actual V13 sheet names
- DRAFT 1 column positions mapped V12->V13 to prevent formula misalignment
- _xlfn.LET formulas identified for rewrite in Plans 03-05
</success_criteria>

<output>
After completion, create `.planning/phases/09-blueprint-v2/09-01-SUMMARY.md`
</output>
