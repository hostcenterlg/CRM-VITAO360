---
phase: 09-blueprint-v2
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - data/output/CRM_VITAO360_V13_PROJECAO.xlsx
  - scripts/phase09_blueprint_v2/02_create_supporting_tabs.py
autonomous: true

must_haves:
  truths:
    - "REGRAS tab exists in V13 with 17 sections (283 rows), 63 motor combinations, and all reference lookup tables"
    - "DRAFT 1 tab exists in V13 with correct column headers matching the column map from Plan 01"
    - "DRAFT 2 tab exists in V13 with correct headers for operational cycle (at minimum: DATA, CONSULTOR, RESULTADO, CNPJ)"
    - "All 3 new tabs have headers that match the formula references CARTEIRA will use"
  artifacts:
    - path: "data/output/CRM_VITAO360_V13_PROJECAO.xlsx"
      provides: "V13 with REGRAS + DRAFT 1 + DRAFT 2 tabs added (total 8 tabs)"
    - path: "scripts/phase09_blueprint_v2/02_create_supporting_tabs.py"
      provides: "Reproducible supporting tab creation script"
  key_links:
    - from: "REGRAS tab"
      to: "CARTEIRA TEMPERATURA formula"
      via: "INDEX/MATCH on rows 220-282 (motor 63 combinations)"
      pattern: "REGRAS!\\$G\\$220"
    - from: "DRAFT 1 tab"
      to: "CARTEIRA MERCOS block formulas"
      via: "INDEX/MATCH by CNPJ in column B"
      pattern: "DRAFT 1.*MATCH.*\\$B"
    - from: "DRAFT 2 tab"
      to: "CARTEIRA FUNIL block formulas"
      via: "CSE array formula finding most recent record by CNPJ"
      pattern: "DRAFT 2.*MAX.*IF"
---

<objective>
Create the 3 supporting tabs (REGRAS, DRAFT 1, DRAFT 2) in V13 that CARTEIRA formulas will reference. Without these tabs existing BEFORE CARTEIRA is built, every formula would return #REF!.

Purpose: CARTEIRA is a formula sheet -- almost every cell references DRAFT 1, DRAFT 2, REGRAS, or PROJECAO. The supporting tabs MUST exist first (even with just headers) so formulas can be written with valid references. REGRAS needs full data (17 sections, 63 motor combinations). DRAFT 1 needs headers + existing client data from Phase 3 output. DRAFT 2 needs headers + structure for daily operational cycle.

Output: V13 file with 8 tabs (5 existing + 3 new), ready for CARTEIRA construction.
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-blueprint-v2/09-RESEARCH.md
@.planning/phases/09-blueprint-v2/09-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create REGRAS tab with 17 sections and 63-combination motor</name>
  <files>
    data/output/CRM_VITAO360_V13_PROJECAO.xlsx
    scripts/phase09_blueprint_v2/02_create_supporting_tabs.py
  </files>
  <action>
Create a Python script that opens V13 (data/output/CRM_VITAO360_V13_PROJECAO.xlsx, data_only=False) and creates 3 new tabs.

STEP 1: Create REGRAS tab by extracting ALL data from V12 COM_DADOS REGRAS tab.

Open V12 (data/sources/crm-versoes/v11-v12/CRM_INTELIGENTE_VITAO360_V12_COM_DADOS.xlsx) and read the REGRAS tab completely (all 283 rows, all columns up to column M). Copy ALL cell values (not formulas -- REGRAS is a reference table with static data) into a new REGRAS tab in V13.

The 17 sections from research must all be present:
1. RESULTADO (rows 5-20): 14 result types with follow-up days
2. TIPO DO CONTATO (23-30): 7 contact types
3. MOTIVO (33-55): 22 motivos with owner
4. SITUACAO (58-65): 7 status values with colors
5. FASE (68-77): 9 lifecycle phases
6. TIPO CLIENTE (80-86): 6 maturity types
7. CONSULTOR (89-94): 5 team members
8. TENTATIVA (97-103): 6 contact protocol steps
9. SINALEIRO (106-110): 4 health indicator colors
10. LISTAS SIMPLES (113-130): 5 dropdown lists
11. TIPO ACAO (133-139): 6 action types
12. TIPO PROBLEMA (142-150): 8 RNC categories
13. ACAO FUTURA (153-200): 48 future actions (22+26)
14. TAREFA/DEMANDA (section 14): 25 internal tasks
15. SINALEIRO META (202-207): 4 achievement bands
16. SCORE RANKING (209-216): 6 weighted factors with percentages (30%, 25%, 20%, 15%, 5%, 5%)
17. MOTOR DE REGRAS (219-283): 63 SITUACAO x RESULTADO combinations

For the MOTOR DE REGRAS section (rows 219-283), ensure ALL 7 columns are copied:
- A: SITUACAO
- B: RESULTADO
- C: ESTAGIO FUNIL
- D: FASE
- E: TIPO CONTATO
- F: ACAO FUTURA
- G: TEMPERATURA

Apply basic formatting: bold headers per section, column widths.

STEP 2: Create DRAFT 1 tab in V13.

The V13 already has an existing DRAFT 1 in the V12 file (data/sources/drafts/). But V13 itself does NOT have a DRAFT 1 tab. Create it using the column structure from draft1_column_map.json (Plan 01 output).

Read the existing DRAFT 1 from V12 COM_DADOS:
- Copy ALL headers (row 3 in V12)
- Copy ALL data rows (554 clients from Phase 3)
- Preserve CNPJ as string (14-digit zfill)
- Ensure column positions match what CARTEIRA formulas will reference

Key columns that CARTEIRA INDEX/MATCH formulas need (from research):
- $B: CNPJ (match column)
- DIAS SEM COMPRA, CICLO MEDIO, N COMPRAS, CURVA ABC, MESES POSITIVADO, MEDIA MENSAL, TICKET MEDIO, TIPO CLIENTE
- Monthly sales columns
- E-commerce columns (15-20, populated in Phase 6)

Use the draft1_column_map.json from Plan 01 to ensure correct column alignment.

STEP 3: Create DRAFT 2 tab in V13.

DRAFT 2 is the operational staging area for daily atendimento results. Structure from V12 audit:
- Col A: DATA (date of contact)
- Col B: ID (auto-increment or blank)
- Col C: CONSULTOR
- Col D: CNPJ (14-digit string -- this is the MATCH column for CARTEIRA formulas)
- Col E: NOME FANTASIA
- Col F: SITUACAO
- Col G: RESULTADO
- Col H: MOTIVO
- Col I: ESTAGIO FUNIL (lookup from REGRAS)
- Col J-onwards: additional fields per V12 audit

Initially DRAFT 2 will have ONLY headers (row 3) and no data rows. The daily operational cycle will populate it. But headers MUST be correct so CARTEIRA formulas can reference the right columns.

Also populate DRAFT 2 with the 6,775 historical records from DRAFT2_POPULADO_DADOS_REAIS_v3.xlsx (data/sources/crm-versoes/v11-v12/ or discovered via find). This gives the CARTEIRA real data to calculate against.

CRITICAL: Before saving, verify ALL existing 5 tabs still have their data intact (especially PROJECAO 19,224 formulas).

Python: /c/Users/User/.pyenv/pyenv-win/pyenv-win/versions/3.12.10/python.exe
  </action>
  <verify>
Run the script: /c/Users/User/.pyenv/pyenv-win/pyenv-win/versions/3.12.10/python.exe scripts/phase09_blueprint_v2/02_create_supporting_tabs.py

Verify:
1. V13 now has 8 tabs: PROJECAO, LOG, DASH, REDES_FRANQUIAS_v2, COMITE, REGRAS, DRAFT 1, DRAFT 2
2. REGRAS tab: >=283 rows, MOTOR section rows 219-283 have data in cols A-G
3. DRAFT 1 tab: >=554 data rows, CNPJ in col B as string
4. DRAFT 2 tab: headers in row 3, CNPJ column in expected position (col D)
5. PROJECAO still has 19,224 formulas (load with data_only=False, count non-None cells with "=" prefix)
6. No #REF! errors introduced
  </verify>
  <done>
V13 has 8 tabs. REGRAS contains all 17 sections including 63-combination motor. DRAFT 1 has 554 clients with correct column positions for CARTEIRA INDEX/MATCH. DRAFT 2 has proper headers and historical data. All 5 prior tabs intact.
  </done>
</task>

<task type="auto">
  <name>Task 2: Validate supporting tab integrity and formula reference readiness</name>
  <files>
    data/output/phase09/supporting_tabs_validation.json
  </files>
  <action>
Create a validation check that verifies the 3 new tabs are ready for CARTEIRA formula injection:

1. REGRAS validation:
   - Count rows in motor section (219-283): must be exactly 63 (or close)
   - Verify all 7 SITUACAO values present: ATIVO, EM RISCO, INAT.REC, INAT.ANT, PROSPECT, NOVO, LEAD
   - Verify SCORE RANKING section (209-216) has 6 factors with weights summing to 100%
   - Spot-check: ATIVO+EM ATENDIMENTO -> TEMPERATURA should be "MORNO"

2. DRAFT 1 validation:
   - Verify CNPJ column position matches draft1_column_map.json
   - Sample 5 CNPJs and check they exist in PROJECAO tab
   - Verify monthly sales columns are populated (non-zero sum)
   - Verify e-commerce columns (15-20) have data for matched clients

3. DRAFT 2 validation:
   - Verify column D is CNPJ
   - If historical data populated: verify row count >= 6,000
   - Verify DATA column has date-like values
   - Verify CONSULTOR column has known consultant names

4. Cross-tab reference check:
   - For each formula pattern in v12_formula_audit.json that references DRAFT 1, verify the target column EXISTS in V13 DRAFT 1
   - For each formula pattern referencing DRAFT 2, verify the target column EXISTS in V13 DRAFT 2
   - For each formula pattern referencing REGRAS, verify the target cell range EXISTS

Output supporting_tabs_validation.json with pass/fail per check.
  </action>
  <verify>
Run validation and check JSON output.

All critical checks must PASS:
1. REGRAS motor has 63 combinations
2. DRAFT 1 CNPJ in correct column
3. DRAFT 2 headers match expected structure
4. Cross-tab references all resolve
  </verify>
  <done>
All 3 supporting tabs validated. Formula references from CARTEIRA will resolve correctly. No #REF! risk. Ready for Plan 03 (CARTEIRA skeleton).
  </done>
</task>

</tasks>

<verification>
1. V13 has exactly 8 tabs (5 original + 3 new)
2. REGRAS: 17 sections, 63 motor combinations, SCORE RANKING weights = 100%
3. DRAFT 1: 554+ data rows, CNPJ in col B, monthly sales populated
4. DRAFT 2: headers correct, historical data loaded if available
5. PROJECAO 19,224 formulas intact
6. Cross-tab formula references all validate
</verification>

<success_criteria>
- V13 file has 8 tabs, all accessible without errors
- REGRAS tab is a complete replica of V12 REGRAS data
- DRAFT 1 column positions match the mapping from Plan 01
- DRAFT 2 structure matches what CARTEIRA FUNIL formulas expect
- 19,224 PROJECAO formulas preserved
</success_criteria>

<output>
After completion, create `.planning/phases/09-blueprint-v2/09-02-SUMMARY.md`
</output>
