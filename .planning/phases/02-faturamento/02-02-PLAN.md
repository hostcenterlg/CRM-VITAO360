---
phase: 02-faturamento
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - scripts/phase02_faturamento/03_merge_sap_mercos.py
  - scripts/phase02_faturamento/04_populate_carteira.py
  - data/output/phase02/sap_mercos_merged.json
  - data/output/CRM_VITAO360_V13_PROJECAO.xlsx
autonomous: true

must_haves:
  truths:
    - "Dados SAP e Mercos combinados com estrategia SAP-First sem dupla contagem"
    - "Clientes exclusivos SAP usam valores SAP, clientes exclusivos Mercos usam valores Mercos"
    - "Clientes em ambos: SAP como base, Mercos como fallback para meses onde SAP tem 0"
    - "10 clientes Sem CNPJ tentados via fuzzy match contra 08_CARTEIRA_MERCOS e SAP Cadastro"
    - "Colunas 26-36 da CARTEIRA (MAR/25 a JAN/26) populadas com vendas merged para clientes encontrados"
    - "Colunas de formula (14, 15, 16, 24, 25, 37, 39, 41, 42) NAO sobrescritas"
  artifacts:
    - path: "scripts/phase02_faturamento/03_merge_sap_mercos.py"
      provides: "Merge SAP-First + Mercos-Complement com fuzzy match de clientes sem CNPJ"
      contains: "merge_sap_mercos"
    - path: "scripts/phase02_faturamento/04_populate_carteira.py"
      provides: "Populacao de vendas mensais na CARTEIRA V13"
      contains: "populate_carteira"
    - path: "data/output/phase02/sap_mercos_merged.json"
      provides: "JSON com dados combinados SAP+Mercos por CNPJ"
    - path: "data/output/CRM_VITAO360_V13_PROJECAO.xlsx"
      provides: "V13 atualizado com vendas na CARTEIRA"
  key_links:
    - from: "scripts/phase02_faturamento/03_merge_sap_mercos.py"
      to: "data/output/phase02/mercos_vendas.json"
      via: "json.load"
      pattern: "mercos_vendas\\.json"
    - from: "scripts/phase02_faturamento/03_merge_sap_mercos.py"
      to: "data/output/phase02/sap_vendas.json"
      via: "json.load"
      pattern: "sap_vendas\\.json"
    - from: "scripts/phase02_faturamento/04_populate_carteira.py"
      to: "data/output/CRM_VITAO360_V13_PROJECAO.xlsx"
      via: "openpyxl load_workbook data_only=False"
      pattern: "load_workbook.*V13"
---

<objective>
Combinar vendas SAP e Mercos com estrategia SAP-First (sem dupla contagem), tentar resolver os 10 clientes Mercos sem CNPJ, e popular as colunas de vendas mensais (26-36) na aba CARTEIRA do arquivo V13 gerado pela Phase 1.

Purpose: Produzir o dataset consolidado de faturamento por CNPJ+mes e ESCREVER na CARTEIRA do CRM. A CARTEIRA V12/V13 tem apenas 3 linhas de dados -- esta populacao e critica para que as formulas de ranking/sinaleiro (que ja existem) possam calcular.

Output:
- `data/output/phase02/sap_mercos_merged.json` -- Dados combinados com stats de merge
- `data/output/CRM_VITAO360_V13_PROJECAO.xlsx` -- V13 com CARTEIRA populada (colunas 26-36)
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-faturamento/02-RESEARCH.md
@.planning/phases/02-faturamento/02-01-SUMMARY.md

# Fontes de dados para fuzzy match dos 10 sem CNPJ:
# - data/sources/mercos/08_CARTEIRA_MERCOS.xlsx (aba "Carteira Clientes Mercos" com CNPJ + Nome Fantasia)
# - data/sources/sap/01_SAP_CONSOLIDADO.xlsx (aba "Cadastro Clientes SAP" com CNPJ + Nome)
#
# CRM V13 (output da Phase 1):
# - data/output/CRM_VITAO360_V13_PROJECAO.xlsx
# - CARTEIRA: col 2(B) = CNPJ, cols 26-36 = vendas MAR/25..JAN/26
# - CARTEIRA quase vazia (3 rows de dados) -- precisamos checar quantos CNPJs tem na col B
#
# MAPEAMENTO CARTEIRA:
# Col 26=MAR/25, 27=ABR/25, 28=MAI/25, 29=JUN/25, 30=JUL/25
# Col 31=AGO/25, 32=SET/25, 33=OUT/25, 34=NOV/25, 35=DEZ/25, 36=JAN/26
# Col 25=TOTAL PERIODO (FORMULA SUM -- NAO SOBRESCREVER)
# Colunas PROIBIDAS (formulas): 14, 15, 16, 24, 25, 37, 39, 41, 42
</context>

<tasks>

<task type="auto">
  <name>Task 1: Merge SAP-First + Mercos-Complement com fuzzy match dos 10 sem CNPJ</name>
  <files>
    scripts/phase02_faturamento/03_merge_sap_mercos.py
    data/output/phase02/sap_mercos_merged.json
  </files>
  <action>
Criar script `scripts/phase02_faturamento/03_merge_sap_mercos.py` que:

1. Carrega os JSONs produzidos pelo Plan 02-01:
   - `data/output/phase02/mercos_vendas.json` -> mercos_data
   - `data/output/phase02/sap_vendas.json` -> sap_data

2. MERGE SAP-FIRST (estrategia confirmada no RESEARCH):
   ```python
   merged = {}
   stats = {"sap_only": 0, "mercos_only": 0, "both_sap_base": 0}

   all_cnpjs = set(sap_data["cnpj_to_vendas"].keys()) | set(mercos_data["cnpj_to_vendas"].keys())
   for cnpj in all_cnpjs:
       sap = sap_data["cnpj_to_vendas"].get(cnpj)
       mercos = mercos_data["cnpj_to_vendas"].get(cnpj)

       if sap and mercos:
           # Ambos: SAP como base. Para meses onde SAP=0 mas Mercos>0, usar Mercos
           merged[cnpj] = [s if s > 0 else m for s, m in zip(sap, mercos)]
           stats["both_sap_base"] += 1
       elif sap:
           merged[cnpj] = sap
           stats["sap_only"] += 1
       else:
           merged[cnpj] = mercos
           stats["mercos_only"] += 1
   ```

3. TRATAR JAN/26 (fora do array de 12 meses 2025):
   - JAN/26 vem apenas do Mercos (SAP nao tem)
   - Guardar separado: jan26_vendas do mercos_data
   - Para clientes merged que tem jan26_vendas, incluir no JSON

4. FUZZY MATCH dos 10 clientes "Sem CNPJ":
   - Carregar `data/sources/mercos/08_CARTEIRA_MERCOS.xlsx` aba "Carteira Clientes Mercos" (Nome Fantasia + CNPJ)
   - Carregar `data/sources/sap/01_SAP_CONSOLIDADO.xlsx` aba "Cadastro Clientes SAP" (Nome + CNPJ)
   - Para cada cliente sem CNPJ da lista mercos_data["sem_cnpj"]:
     a. Buscar match exato por nome (uppercase, strip) no Carteira Mercos
     b. Se nao encontrar, buscar match exato no SAP Cadastro
     c. Se nao encontrar, tentar match parcial (nome contido no outro ou vice-versa)
     d. NOTA: "60.641.605 LARA DE BARROS AMAN" provavelmente e CPF -- extrair digitos e normalizar
     e. "DIVINA TERRA CURITIBA" -- buscar por "DIVINA TERRA" como prefixo
   - Se match encontrado com CNPJ, adicionar vendas ao merged
   - Se NAO encontrado, registrar no JSON como "unmatched" com valor total (para gap analysis)
   - NAO instalar rapidfuzz (evitar dependencia extra). Usar string matching simples e manual.

5. Calcular totais:
   - total_merged_2025 = sum of all monthly values
   - Esperado: ~R$ 2.149.389 (SAP+Mercos combinado do HANDOFF)
   - Listar quantos clientes ficaram merged, quantos sap_only, mercos_only, matched_sem_cnpj, unmatched

6. Salvar JSON:
```json
{
  "cnpj_to_vendas": {"CNPJ": [jan, fev, mar, ..., dez], ...},
  "jan26_vendas": {"CNPJ": valor, ...},
  "unmatched": [{"nome": "...", "total": 1234.56, "reason": "no CNPJ match found"}, ...],
  "fuzzy_matches": [{"nome": "...", "matched_cnpj": "...", "match_type": "exact_name|partial|cpf"}, ...],
  "stats": {
    "sap_only": 300,
    "mercos_only": 150,
    "both_sap_base": 200,
    "fuzzy_matched": 5,
    "unmatched": 5,
    "total_clientes": 655,
    "total_vendas_2025": 2149389.00,
    "total_jan26": 114038.03
  },
  "source": "SAP-First + Mercos-Complement"
}
```
  </action>
  <verify>
Rodar: `/c/Users/User/.pyenv/pyenv-win/pyenv-win/versions/3.12.10/python.exe scripts/phase02_faturamento/03_merge_sap_mercos.py`

Verificar:
1. Script roda sem erros
2. JSON criado em data/output/phase02/sap_mercos_merged.json
3. stats.total_clientes > 600 (combinacao SAP+Mercos unica por CNPJ)
4. stats.total_vendas_2025 proximo de R$ 2.149.389 (tolerancia 1%)
5. stats.sap_only + stats.mercos_only + stats.both_sap_base = stats.total_clientes - stats.fuzzy_matched
6. Nenhum CNPJ aparece com valor negativo
7. fuzzy_matches lista mostra quais dos 10 foram resolvidos
  </verify>
  <done>
JSON sap_mercos_merged.json existe com dados combinados SAP-First, total ~R$ 2.149k (±1%), fuzzy match tentado para os 10 sem CNPJ, stats completas de merge, e JAN/26 separado.
  </done>
</task>

<task type="auto">
  <name>Task 2: Popular colunas de vendas mensais (26-36) na CARTEIRA do V13</name>
  <files>
    scripts/phase02_faturamento/04_populate_carteira.py
    data/output/CRM_VITAO360_V13_PROJECAO.xlsx
  </files>
  <action>
Criar script `scripts/phase02_faturamento/04_populate_carteira.py` que:

1. Carrega o JSON merged: `data/output/phase02/sap_mercos_merged.json`
2. Abre o V13 com openpyxl (data_only=False -- PRESERVAR FORMULAS):
   `data/output/CRM_VITAO360_V13_PROJECAO.xlsx`
3. Encontra a aba "CARTEIRA" (usar find_sheet).

4. INVESTIGAR CARTEIRA PRIMEIRO (antes de popular):
   - Contar quantas rows tem dados na col B (CNPJ)
   - Se apenas 3 rows de dados (como documentado): a CARTEIRA esta quase vazia
   - DECISAO: Se CARTEIRA tem poucos CNPJs, NAO podemos popular vendas sem antes ter os clientes
   - Nesse caso: criar CARTEIRA entries para cada CNPJ do merged (pelo menos CNPJ na col B e nome se disponivel)
   - Se CARTEIRA ja tem CNPJs: fazer match por CNPJ normalizado

5. POPULAR VENDAS (colunas SEGURAS):
   Mapeamento mes -> coluna CARTEIRA:
   ```python
   month_to_col = {
       2: 26,   # MAR/25
       3: 27,   # ABR/25
       4: 28,   # MAI/25
       5: 29,   # JUN/25
       6: 30,   # JUL/25
       7: 31,   # AGO/25
       8: 32,   # SET/25
       9: 33,   # OUT/25
       10: 34,  # NOV/25
       11: 35,  # DEZ/25
   }
   # JAN/26 -> col 36
   ```

   NOTA: JAN/25 (posicao 0) e FEV/25 (posicao 1) NAO tem coluna na CARTEIRA V12.
   - JAN/25: dados so existem no SAP
   - FEV/25: dados existem no SAP e parcial no Mercos
   - Recomendacao do RESEARCH: NAO inserir colunas. Somar JAN+FEV no merged JSON para referencia, mas NAO escrever na CARTEIRA.

6. REGRAS DE ESCRITA:
   - NUNCA sobrescrever colunas de formula: 14, 15, 16, 24, 25, 37, 39, 41, 42
   - Verificar col 25 (TOTAL PERIODO) -- se e formula (começa com =), NAO tocar
   - Para cada CNPJ no merged:
     a. Encontrar a row na CARTEIRA onde col B = mesmo CNPJ
     b. Escrever vendas[2..11] nas colunas 26-35 (MAR-DEZ)
     c. Escrever jan26_vendas nas col 36 se disponivel
     d. Se valor = 0.0, escrever None (celula vazia) em vez de 0

7. TRACKING:
   - Contar: clientes populados, clientes nao encontrados na CARTEIRA, clientes da CARTEIRA sem vendas
   - Print relatorio de populacao

8. SALVAR o V13 atualizado no MESMO caminho: `data/output/CRM_VITAO360_V13_PROJECAO.xlsx`
   - CRIAR BACKUP antes: copiar para `data/output/CRM_VITAO360_V13_PROJECAO_backup_pre_phase02.xlsx`

NOTA CRITICA sobre CARTEIRA quase vazia:
Se a CARTEIRA so tem 3 linhas, o script deve:
- Documentar esta situacao no output/log
- Produzir o JSON merged como artefato principal (usado pelas fases posteriores)
- Popular as 3 linhas que existem como proof-of-concept
- NAO tentar inserir 650+ linhas na CARTEIRA agora (isso e escopo da Phase 9 Blueprint)
- Registrar quantos clientes do merged TEM match na CARTEIRA vs quantos nao tem
  </action>
  <verify>
Rodar: `/c/Users/User/.pyenv/pyenv-win/pyenv-win/versions/3.12.10/python.exe scripts/phase02_faturamento/04_populate_carteira.py`

Verificar:
1. Script roda sem erros
2. Backup criado em data/output/CRM_VITAO360_V13_PROJECAO_backup_pre_phase02.xlsx
3. V13 atualizado salvo com vendas nas colunas 26-36
4. Colunas de formula (25, 37, etc.) NAO foram sobrescritas (verificar com openpyxl que ainda sao formulas)
5. Relatorio mostra quantos clientes populados vs nao encontrados
6. Se CARTEIRA quase vazia: documentado no log com contagem de rows disponiveis
  </verify>
  <done>
V13 salvo com vendas populadas nas colunas 26-36 da CARTEIRA para todos os clientes encontrados, formulas preservadas, backup criado, e relatorio de populacao indicando quantos clientes matched/unmatched.
  </done>
</task>

</tasks>

<verification>
1. sap_mercos_merged.json contém dados combinados SAP-First com total ~R$ 2.149k
2. Fuzzy match tentado para 10 clientes sem CNPJ, resultados documentados
3. V13 CARTEIRA populada com vendas mensais (colunas 26-36) sem destruir formulas
4. Backup do V13 pre-Phase 2 criado
5. JAN/25 e FEV/25 NAO escritos na CARTEIRA (nao tem colunas) -- documentado
6. Nenhuma coluna de formula sobrescrita
</verification>

<success_criteria>
- Dados SAP+Mercos combinados com zero dupla contagem (SAP-First validado)
- Pelo menos 5 dos 10 clientes sem CNPJ resolvidos via matching
- CARTEIRA do V13 populada nas colunas corretas (26-36)
- Todas as formulas do V13 preservadas intactas
- JSON merged disponivel como intermediario para Plan 02-03 e fases posteriores
</success_criteria>

<output>
After completion, create `.planning/phases/02-faturamento/02-02-SUMMARY.md`
</output>
