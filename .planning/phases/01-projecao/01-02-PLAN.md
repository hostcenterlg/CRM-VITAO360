---
phase: 01-projecao
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - scripts/phase01_projecao/03_populate_projecao.py
  - data/output/CRM_VITAO360_V13_PROJECAO.xlsx
autonomous: true

must_haves:
  truths:
    - "Colunas de dados (A-E, L-X, AA-AL) populadas com dados SAP 2026 atualizados"
    - "Colunas de formula (F-J, Z, AN-AQ, BB-CB) NUNCA tocadas — intactas"
    - "534 clientes com CNPJ como chave de cruzamento"
    - "Metas mensais (M-X) derivadas de META ANUAL (L) usando pesos SAP"
    - "Vendas realizadas 2025 (AA-AL) populadas do SAP Consolidado"
    - "Arquivo salvo preserva todas as 19.224 formulas"
  artifacts:
    - path: "scripts/phase01_projecao/03_populate_projecao.py"
      provides: "Script de populacao Read-Modify-Write da PROJECAO"
      min_lines: 150
    - path: "data/output/CRM_VITAO360_V13_PROJECAO.xlsx"
      provides: "PROJECAO populada com dados SAP 2026 (output principal)"
  key_links:
    - from: "scripts/phase01_projecao/03_populate_projecao.py"
      to: "data/output/phase01/sap_data_extracted.json"
      via: "json.load para dados SAP"
      pattern: "sap_data_extracted\\.json"
    - from: "scripts/phase01_projecao/03_populate_projecao.py"
      to: "data/sources/projecao/PROJECAO_534_INTEGRADA.xlsx"
      via: "openpyxl load_workbook(data_only=False) Read-Modify-Write"
      pattern: "load_workbook.*data_only.*False"
---

<objective>
Popular a PROJECAO_534_INTEGRADA com dados SAP 2026 atualizados (metas anuais, metas mensais, vendas realizadas 2025) usando o padrao Read-Modify-Write que preserva todas as 19.224 formulas. Gerar o arquivo output principal V13.

Purpose: Esta e a entrega central da Fase 1 — transformar a PROJECAO de "formulas com dados antigos" para "formulas com dados SAP 2026 frescos". As formulas recalculam automaticamente (% YTD, sinaleiro, GAP, ranking) com base nos novos dados.

Output: `data/output/CRM_VITAO360_V13_PROJECAO.xlsx` com dados atualizados e formulas intactas.
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-projecao/01-RESEARCH.md
@.planning/phases/01-projecao/01-01-SUMMARY.md

Source files:
@data/sources/projecao/PROJECAO_534_INTEGRADA.xlsx
@data/output/phase01/sap_data_extracted.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Popular PROJECAO com dados SAP 2026 via Read-Modify-Write</name>
  <files>
    scripts/phase01_projecao/03_populate_projecao.py
    data/output/CRM_VITAO360_V13_PROJECAO.xlsx
  </files>
  <action>
Criar script `scripts/phase01_projecao/03_populate_projecao.py` que:

**Setup:**
```python
import openpyxl
import json
import os
import re
import shutil
from copy import copy

PYTHON = '/c/Users/User/.pyenv/pyenv-win/pyenv-win/versions/3.12.10/python.exe'
PROJECT = 'c:/Users/User/OneDrive/Documentos/GitHub/CRM-VITAO360'
SOURCE = os.path.join(PROJECT, 'data/sources/projecao/PROJECAO_534_INTEGRADA.xlsx')
SAP_DATA = os.path.join(PROJECT, 'data/output/phase01/sap_data_extracted.json')
OUTPUT = os.path.join(PROJECT, 'data/output/CRM_VITAO360_V13_PROJECAO.xlsx')
```

**1. Carregar dados SAP do JSON intermediario (Plan 01):**
```python
with open(SAP_DATA, 'r') as f:
    sap = json.load(f)
vendas_2025 = sap['cnpj_to_vendas_2025']
metas_2026 = sap['cnpj_to_meta_2026']
weights = sap['monthly_weights']
```

**2. Abrir PROJECAO preservando formulas:**
```python
# CRITICO: data_only=False para preservar formulas
wb = openpyxl.load_workbook(SOURCE, data_only=False)
ws = wb['PROJECAO ']  # Espaco no final!
```

**3. Funcao normalize_cnpj:**
```python
def normalize_cnpj(raw):
    if raw is None: return None
    clean = re.sub(r'[^0-9]', '', str(raw))
    return clean.zfill(14) if len(clean) <= 14 else clean
```

**4. Para cada linha de dados (rows 4-537), popular:**

a) **Coluna L (META ANUAL, col 12):**
   - Buscar CNPJ da celula A{row}, normalizar
   - Se CNPJ encontrado em metas_2026: setar ws.cell(row, 12).value = meta_anual
   - Se nao encontrado: manter valor existente (nao zerar)
   - Formato numero: `'_-"R$"\\ * #,##0.00_-;\\-"R$"\\ * #,##0.00_-;_-"R$"\\ * "-"??_-;_-@_-'`

b) **Colunas M-X (META MENSAL JAN-DEZ, cols 13-24):**
   - Para cada mes i (0-11): ws.cell(row, 13+i).value = meta_anual * weights[i]
   - Mesmo formato R$ que coluna L
   - Apenas popular se meta_anual > 0

c) **Colunas AA-AL (REALIZADO JAN-DEZ, cols 27-38):**
   - Se CNPJ encontrado em vendas_2025: popular vendas mensais
   - ws.cell(row, 27+i).value = vendas[i] para i in range(12)
   - Se nao encontrado: setar 0 (sem vendas registradas)
   - Mesmo formato R$

**5. NUNCA tocar nas seguintes colunas (formulas):**
   - F-J (cols 6-10): VLOOKUPs do sinaleiro
   - Z (col 26): =SUM(AA:AL) -- recalcula automaticamente
   - AN-AQ (cols 40-43): Indicadores (% YTD, sinaleiro, GAP, ranking)
   - BB-CB (cols 54-80): Meta igualitaria e compensada

**6. Preservar propriedades da aba:**
   - freeze_panes = 'C4' (nao alterar)
   - auto_filter mantido (nao alterar)
   - Column widths mantidos
   - Row heights mantidos

**7. Criar diretorio output se nao existir e salvar:**
```python
os.makedirs(os.path.dirname(OUTPUT), exist_ok=True)
wb.save(OUTPUT)
```

**8. Imprimir estatisticas:**
   - Clientes processados: N de 534
   - Clientes com meta atualizada: N
   - Clientes com vendas populadas: N
   - Clientes sem match (CNPJ nao encontrado): N (listar primeiros 10)
   - Meta total populada: R$ X
   - Vendas total populadas: R$ X

**ATENCAO CRITICA:**
- NUNCA usar `data_only=True` ao abrir o arquivo -- destroi formulas
- NUNCA escrever em colunas de formula (F-J, Z, AN-AQ, BB-CB)
- CNPJ sempre como string 14 digitos (normalize_cnpj)
- Se valor de venda/meta for None ou nao-numerico, tratar como 0
- Preservar o formato R$ em todas as celulas de valor monetario
  </action>
  <verify>
Rodar: `/c/Users/User/.pyenv/pyenv-win/pyenv-win/versions/3.12.10/python.exe scripts/phase01_projecao/03_populate_projecao.py`

Verificacoes:
1. Arquivo `data/output/CRM_VITAO360_V13_PROJECAO.xlsx` criado com sucesso
2. Console mostra: clientes processados ~534, metas atualizadas >0, vendas populadas >0
3. Meta total proxima de R$ 4.747.200

Verificacao adicional (abrir o output com openpyxl data_only=False e checar):
- Formulas em F4 comecam com `=IFERROR(VLOOKUP`
- Formula em Z4 = `=SUM(AA4:AL4)`
- Formula em AN4 = `=IF(L4=0,0,Z4/L4)`
- Valores em L4, AA4 sao numericos (nao formulas)
  </verify>
  <done>
PROJECAO populada com dados SAP 2026: metas anuais na coluna L, metas mensais em M-X (usando pesos SAP), vendas realizadas 2025 em AA-AL. Todas as 19.224 formulas preservadas intactas. Arquivo output gerado em data/output/CRM_VITAO360_V13_PROJECAO.xlsx.
  </done>
</task>

</tasks>

<verification>
1. Arquivo output existe e tem tamanho > 300KB
2. Aba PROJECAO tem 537 rows e 80 columns
3. Formulas intactas (spot check em F4, Z4, AN4, BB4, BP4)
4. Valores de META populados em L4 (numerico, nao formula)
5. Valores de REALIZADO populados em AA4 (numerico)
6. Freeze panes = C4 mantido
</verification>

<success_criteria>
- Arquivo V13 gerado com dados SAP 2026 populados
- 19.224 formulas preservadas (verificar amostra de cada bloco)
- Metas mensais calculadas com pesos SAP (soma = meta anual)
- Vendas 2025 populadas para clientes com historico
- Script reproduzivel e idempotente
</success_criteria>

<output>
After completion, create `.planning/phases/01-projecao/01-02-SUMMARY.md`
</output>
