---
phase: 01-projecao
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/phase01_projecao/01_validate_formulas.py
  - scripts/phase01_projecao/02_extract_sap_data.py
  - data/output/phase01/formula_validation_report.json
  - data/output/phase01/sap_data_extracted.json
autonomous: true

must_haves:
  truths:
    - "19.224 formulas existentes na PROJECAO_534_INTEGRADA estao 100% intactas e validadas"
    - "Mapeamento CNPJ <-> SAP Code extraido com 1.698 registros do SAP Consolidado"
    - "Vendas mensais 2025 extraidas por CNPJ (493 clientes) do SAP Consolidado"
    - "Metas 2026 extraidas por Grupo Chave do BASE_SAP_META_PROJECAO_2026"
    - "Dados intermediarios salvos em JSON para consumo pelo Plan 02"
  artifacts:
    - path: "scripts/phase01_projecao/01_validate_formulas.py"
      provides: "Validacao completa das 19.224 formulas da PROJECAO"
      min_lines: 80
    - path: "scripts/phase01_projecao/02_extract_sap_data.py"
      provides: "Extracao de dados SAP (mapeamento CNPJ, vendas 2025, metas 2026)"
      min_lines: 120
    - path: "data/output/phase01/formula_validation_report.json"
      provides: "Relatorio de validacao com contagem de formulas por bloco"
    - path: "data/output/phase01/sap_data_extracted.json"
      provides: "Dados SAP intermediarios para populacao"
  key_links:
    - from: "scripts/phase01_projecao/01_validate_formulas.py"
      to: "data/sources/projecao/PROJECAO_534_INTEGRADA.xlsx"
      via: "openpyxl load_workbook(data_only=False)"
      pattern: "load_workbook.*data_only.*False"
    - from: "scripts/phase01_projecao/02_extract_sap_data.py"
      to: "data/sources/sap/01_SAP_CONSOLIDADO.xlsx"
      via: "openpyxl load_workbook(data_only=True)"
      pattern: "01_SAP_CONSOLIDADO"
---

<objective>
Validar que as 19.224 formulas existentes na PROJECAO_534_INTEGRADA estao 100% intactas, e extrair todos os dados SAP necessarios (mapeamento CNPJ, vendas 2025, metas 2026) em formato intermediario JSON para consumo pelo Plan 02.

Purpose: Confirmar que a base de formulas esta solida ANTES de popular dados (evitar corromper formulas). Preparar os dados SAP limpos e normalizados para populacao eficiente.

Output: Relatorio de validacao de formulas + dados SAP extraidos em JSON.
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-projecao/01-RESEARCH.md

Source files:
@data/sources/projecao/PROJECAO_534_INTEGRADA.xlsx
@data/sources/sap/01_SAP_CONSOLIDADO.xlsx
@data/sources/sap/BASE_SAP_META_PROJECAO_2026.xlsx
@data/sources/sap/BASE_SAP_VENDA_MES_A_MES_2025.xlsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validar 19.224 formulas da PROJECAO_534_INTEGRADA</name>
  <files>
    scripts/phase01_projecao/01_validate_formulas.py
    data/output/phase01/formula_validation_report.json
  </files>
  <action>
Criar script `scripts/phase01_projecao/01_validate_formulas.py` que:

1. Abre `data/sources/projecao/PROJECAO_534_INTEGRADA.xlsx` com `openpyxl.load_workbook(data_only=False)`.
2. Acessa aba `"PROJECAO "` (com espaco no final -- CRITICO).
3. Valida estrutura basica: max_row=537, max_column=80, freeze_panes='C4'.
4. Para cada uma das 534 linhas de dados (rows 4-537), valida as 36 colunas de formulas:

   **Bloco Sinaleiro (F-J, 5 colunas):**
   - F: `=IFERROR(VLOOKUP(C{r},$AS$4:$AT$18,2,FALSE),"")`
   - G: `=IFERROR(VLOOKUP(C{r},$AS$4:$AU$18,3,FALSE),"")`
   - H: `=IFERROR(VLOOKUP(C{r},$AS$4:$AV$18,4,FALSE),"")`
   - I: `=IFERROR(VLOOKUP(C{r},$AS$4:$AW$18,5,FALSE),"")`
   - J: `=IFERROR(VLOOKUP(C{r},$AS$4:$AX$18,6,FALSE),"")`

   **Realizado Total (Z, 1 coluna):**
   - Z: `=SUM(AA{r}:AL{r})`

   **Indicadores (AN-AQ, 4 colunas):**
   - AN: `=IF(L{r}=0,0,Z{r}/L{r})`
   - AO: `=IF(AN{r}="","",IF(AN{r}>=0.9,"verde",IF(AN{r}>=0.7,"amarelo",IF(AN{r}>=0.5,"laranja","vermelho"))))`
   - AP: `=IF(L{r}=0,0,L{r}-Z{r})`
   - AQ: `=IF(Z{r}=0,"",RANK(Z{r},Z$4:Z$537,0))`

   **Meta Igualitaria (BB-BN, 13 colunas):**
   - BB: `=SUM(L$4:L$537)/COUNTA(A$4:A$537)`
   - BC-BN: `=SUM({col_letter}$4:{col_letter}$537)/COUNTA(A$4:A$537)` onde col_letter = M ate X

   **Meta Compensada (BP-CB, 13 colunas):**
   - BP: Formula complexa com SUMPRODUCT (validar que comeca com `=IF(Z{r}>=BB{r}`)
   - BQ-CB: Formulas complexas (validar que comecam com `=IF(`)

5. Para cada celula, compara formula esperada vs formula real. Conta matches e mismatches.
6. Valida tambem:
   - 534 CNPJs na coluna A (todos nao-vazios, entre rows 4-537)
   - Tabela auxiliar AS:AZ (rows 4-18, 15 redes)
   - Separadores em E, K, Y, AM, AR, BA, BO (valor = ".")
7. Salva relatorio JSON em `data/output/phase01/formula_validation_report.json` com:
   ```json
   {
     "total_formulas_expected": 19224,
     "total_formulas_found": N,
     "total_formulas_matched": N,
     "total_mismatches": N,
     "mismatches": [...],
     "blocks": {
       "sinaleiro_FJ": {"expected": 2670, "matched": N},
       "realizado_Z": {"expected": 534, "matched": N},
       "indicadores_AN_AQ": {"expected": 2136, "matched": N},
       "meta_igualitaria_BB_BN": {"expected": 6942, "matched": N},
       "meta_compensada_BP_CB": {"expected": 6942, "matched": N}
     },
     "structure": {
       "max_row": N, "max_column": N,
       "freeze_panes": "...",
       "cnpj_count": N,
       "redes_count": N
     }
   }
   ```
8. Imprime resumo no console com pass/fail por bloco.

**Python path:** `/c/Users/User/.pyenv/pyenv-win/pyenv-win/versions/3.12.10/python.exe`
**IMPORTANTE:** Criar diretorio `scripts/phase01_projecao/` e `data/output/phase01/` se nao existirem.
**IMPORTANTE:** Para as formulas de Meta Compensada (BP-CB), validar apenas o INICIO da formula (`=IF(Z{r}>=BB{r}` para BP, `=IF(` para BQ-CB) pois sao formulas muito longas e complexas. O importante e confirmar que SAO formulas (nao valores estaticos).
  </action>
  <verify>
Rodar: `/c/Users/User/.pyenv/pyenv-win/pyenv-win/versions/3.12.10/python.exe scripts/phase01_projecao/01_validate_formulas.py`

Output esperado:
- "Formula validation: 0 mismatches" (ou lista de mismatches para investigar)
- Arquivo `data/output/phase01/formula_validation_report.json` existe e contem todos os campos
- total_formulas_found = 19224 (36 cols * 534 rows)
- Todos os blocos com 100% match
  </verify>
  <done>
19.224 formulas validadas com 0 mismatches. Relatorio JSON confirma integridade de todos os 5 blocos de formulas. Estrutura da aba (537 rows, 80 cols, freeze C4, 534 CNPJs, 15 redes) confirmada.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extrair dados SAP (mapeamento CNPJ, vendas 2025, metas 2026)</name>
  <files>
    scripts/phase01_projecao/02_extract_sap_data.py
    data/output/phase01/sap_data_extracted.json
  </files>
  <action>
Criar script `scripts/phase01_projecao/02_extract_sap_data.py` que extrai 3 conjuntos de dados:

**Funcao auxiliar (topo do script):**
```python
import re
def normalize_cnpj(raw):
    if raw is None: return None
    clean = re.sub(r'[^0-9]', '', str(raw))
    return clean.zfill(14) if len(clean) <= 14 else clean
```

**1. Mapeamento CNPJ <-> SAP Code (Rosetta Stone):**
- Abrir `data/sources/sap/01_SAP_CONSOLIDADO.xlsx` com `data_only=True`
- Sheet: `Cadastro Clientes SAP`
- Extrair: col C (Codigo do Cliente), col E (CNPJ Cliente)
- Normalizar CNPJ com `normalize_cnpj()`
- Resultado: dict `{cnpj_14dig: sap_code_str}` -- esperar ~1.698 mapeamentos
- Tambem extrair col D (Nome Cliente) e col I (Grupo Chave SAP) para referencia

**2. Vendas Mensais 2025 por CNPJ:**
- Mesma workbook `01_SAP_CONSOLIDADO.xlsx`
- Sheet: `Venda Mes a Mes`
- Extrair: col C (CNPJ/CPF), colunas de Faturado mensal
- Colunas Faturado: G(Jan), K(Fev), O(Mar), S(Abr), W(Mai), AA(Jun), AE(Jul), AI(Ago), AM(Set), AQ(Out), AU(Nov), AY(Dez)
  (indices 7, 11, 15, 19, 23, 27, 31, 35, 39, 43, 47, 51)
- Normalizar CNPJ
- Resultado: dict `{cnpj: [jan, fev, ..., dez]}` com floats -- esperar ~493 clientes
- **Agregacao:** Se mesmo CNPJ aparece em multiplas linhas, SOMAR os valores mensais (um cliente pode ter multiplas linhas por diferentes Grupo Chave)

**3. Metas 2026 por Grupo Chave:**
- Abrir `data/sources/sap/BASE_SAP_META_PROJECAO_2026.xlsx` com `data_only=True`
- Sheet: `Meta e Projecao` (ou primeira sheet)
- Identificar: Row com total R$ 4.747.200 (referencia de validacao)
- Extrair metas por Grupo Chave e distribuir para CNPJs individuais:
  - Usar mapeamento Grupo Chave -> CNPJs do SAP Consolidado
  - Se Grupo Chave tem N clientes, dividir meta proporcionalmente (ou igualitariamente se nao houver historico)
- Resultado: dict `{cnpj: meta_anual_2026}` -- esperar meta para ~534 clientes
- **ALTERNATIVA SIMPLIFICADA:** Se a distribuicao por Grupo Chave for muito complexa, extrair as metas individuais JA EXISTENTES na coluna L da PROJECAO_534_INTEGRADA (que ja tem meta por cliente). Neste caso, apenas VALIDAR que a soma bate com o total SAP (~R$ 4.747.200 com tolerancia de 0.67%).

**4. Pesos de distribuicao mensal SAP:**
- Hardcode dos pesos confirmados na pesquisa:
  ```python
  MONTHLY_WEIGHTS = [
      0.07027, 0.07343, 0.07520, 0.07716,  # Jan-Abr
      0.07969, 0.08177, 0.08493, 0.08702,  # Mai-Ago
      0.08898, 0.09119, 0.09359, 0.09676   # Set-Dez
  ]
  assert abs(sum(MONTHLY_WEIGHTS) - 1.0) < 0.001
  ```

**5. Salvar tudo em `data/output/phase01/sap_data_extracted.json`:**
```json
{
  "cnpj_to_sap_code": {"cnpj": "sap_code", ...},
  "cnpj_to_vendas_2025": {"cnpj": [jan, ..., dez], ...},
  "cnpj_to_meta_2026": {"cnpj": meta_anual, ...},
  "monthly_weights": [0.07027, ...],
  "stats": {
    "total_rosetta_mappings": N,
    "total_vendas_clients": N,
    "total_meta_clients": N,
    "meta_total_sum": N,
    "meta_sap_reference": 4747200,
    "meta_discrepancy_pct": N
  }
}
```

**6. Imprimir resumo no console:**
- Mapeamentos Rosetta: N
- Clientes com vendas 2025: N
- Clientes com meta 2026: N
- Meta total vs SAP reference: R$ X vs R$ 4.747.200 (diff %)
  </action>
  <verify>
Rodar: `/c/Users/User/.pyenv/pyenv-win/pyenv-win/versions/3.12.10/python.exe scripts/phase01_projecao/02_extract_sap_data.py`

Output esperado:
- Mapeamentos Rosetta ~1.698
- Clientes com vendas ~493
- Clientes com meta ~534
- Meta total proximo de R$ 4.747.200 (tolerancia 0.67%)
- Arquivo `data/output/phase01/sap_data_extracted.json` existe com todos os campos
  </verify>
  <done>
Mapeamento CNPJ<->SAP com ~1.698 registros extraido. Vendas 2025 por CNPJ com ~493 clientes extraidas. Metas 2026 por CNPJ com ~534 clientes extraidas. Pesos mensais SAP validados (soma=100%). Todos os dados salvos em JSON intermediario para consumo pelo Plan 02.
  </done>
</task>

</tasks>

<verification>
1. Script 01_validate_formulas.py executa sem erros
2. Relatorio JSON confirma 19.224 formulas, 0 mismatches
3. Script 02_extract_sap_data.py executa sem erros
4. JSON de dados SAP contem as 3 tabelas de mapeamento
5. Meta total dentro de tolerancia de 0.67% vs R$ 4.747.200
</verification>

<success_criteria>
- 19.224 formulas validadas como intactas na PROJECAO_534_INTEGRADA
- Dados SAP extraidos e normalizados em JSON intermediario
- Mapeamento CNPJ cobrindo os 534 clientes da PROJECAO
- Nenhuma formula corrompida ou ausente
- Scripts reproduziveis (podem ser re-executados sem side effects)
</success_criteria>

<output>
After completion, create `.planning/phases/01-projecao/01-01-SUMMARY.md`
</output>
