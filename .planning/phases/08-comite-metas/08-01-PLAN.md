---
phase: 08-comite-metas
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/phase08_comite_metas/01_validate_adjust_metas.py
  - data/output/CRM_VITAO360_V13_PROJECAO.xlsx
autonomous: true

must_haves:
  truths:
    - "META ANUAL totals in PROJECAO reconciled against SAP R$ 4,747,200 with delta documented"
    - "REALIZADO data availability mapped (which months have data, which are zero)"
    - "MANU DITZEL alias (10 clients) accounted for in consultant aggregation"
    - "41 orphan clients (sem consultor) identified and documented"
    - "3 meta column sets verified: proportional (L:X), equal (BB:BN), dynamic (BP:CB)"
  artifacts:
    - path: "scripts/phase08_comite_metas/01_validate_adjust_metas.py"
      provides: "Meta validation + consultant mapping + REALIZADO audit + toggle readiness check"
      min_lines: 150
    - path: "data/output/phase08/meta_validation_report.json"
      provides: "JSON report with meta totals per consultant, delta analysis, REALIZADO availability, alias mapping"
  key_links:
    - from: "scripts/phase08_comite_metas/01_validate_adjust_metas.py"
      to: "data/output/CRM_VITAO360_V13_PROJECAO.xlsx"
      via: "openpyxl load with data_only=False + data_only=True"
      pattern: "load_workbook.*data_only"
---

<objective>
Validate existing META infrastructure in PROJECAO, audit REALIZADO data availability, document the R$ 31,803 delta (4,779,003 vs 4,747,200), map consultant aliases (MANU DITZEL vs HEMANUELE), and verify the 3 meta column sets (proportional/equal/dynamic) are ready for COMITE consumption.

Purpose: Research revealed that META columns ALREADY EXIST in PROJECAO from Phase 1. This plan confirms they are correct, documents gaps, and prepares the data contract for Plan 08-02 (COMITE builder). No new columns are created in PROJECAO -- only validation and a JSON report.

Output: meta_validation_report.json with consultant breakdown, delta analysis, REALIZADO month availability, and column set verification.
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-comite-metas/08-RESEARCH.md
@.planning/phases/08-comite-metas/08-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate META infrastructure and generate meta_validation_report.json</name>
  <files>
    scripts/phase08_comite_metas/01_validate_adjust_metas.py
    data/output/phase08/meta_validation_report.json
  </files>
  <action>
Create a Python script that performs a comprehensive read-only audit of the PROJECAO tab META infrastructure. Use pyenv Python: /c/Users/User/.pyenv/pyenv-win/pyenv-win/versions/3.12.10/python.exe

The script must:

1. **Load V13 twice** (data_only=False for formulas, data_only=True for cached values):
   - Path: data/output/CRM_VITAO360_V13_PROJECAO.xlsx
   - Use find_projecao_sheet() pattern (accent-stripping) from Phase 7 to locate the PROJECAO sheet

2. **Audit META columns (3 sets)**:
   - **Proportional (L:X)**: col L = META ANUAL, cols M:X = META JAN-DEZ. Sum col L rows 4-537. Expected ~R$ 4,779,003.
   - **Equal (BB:BN)**: col BB = META ANUAL IGUALITARIA, cols BC:BN = monthly. Sum col BB rows 4-537.
   - **Dynamic/Compensated (BP:CB)**: col BP = META COMPENSADA ANUAL, cols BQ:CB = monthly. Sum col BP rows 4-537.
   - For each set: report total, count of non-zero values, count of formula cells vs static values

3. **Reconcile against SAP R$ 4,747,200**:
   - Calculate delta: PROJECAO total - SAP total = R$ 31,803 (expected)
   - Report delta as absolute and percentage (0.67%)
   - Document cause: "Rounding from proportional distribution in Phase 1"

4. **Map consultants** from PROJECAO col D (rows 4-537):
   - Count clients per consultant name (exact string)
   - Identify MANU alias: "HEMANUELE DITZEL (MANU)" vs "MANU DITZEL"
   - Count orphans (empty/null/blank col D)
   - For each consultant: sum META ANUAL (col L), sum REALIZADO ANUAL (col Z), calculate % atingimento
   - Include "OUTROS/SEM CONSULTOR" row for orphans
   - Expected consultants: HEMANUELE DITZEL (MANU) ~170, LARISSA PADILHA ~212, JULIO GADRET ~63, DAIANE STAVICKI ~47, MANU DITZEL ~1-10, orphans ~41

5. **Audit REALIZADO availability** (cols AA:AL = REAL JAN-DEZ, col Z = REAL ANUAL):
   - For each month column (AA=JAN through AL=DEZ): count how many of the 534 rows have non-zero cached values
   - Report which months have data (expected: only OCT/NOV/DEC 2025 = cols AI/AJ/AK)
   - Calculate total REALIZADO from col Z: expected ~R$ 2,081,030

6. **Verify indicator columns**:
   - col AN (40) = % YTD: check it contains formulas (not static)
   - col AO (41) = SINAL META: check it contains formulas (emoji-based)
   - col AP (42) = GAP: check it contains formulas
   - col AQ (43) = RANKING: check it contains formulas (RANK)
   - Report count of formula cells vs total rows for each

7. **Verify V13 integrity**: count total formulas in PROJECAO sheet (must be >= 19,224)

8. **Output JSON report** to data/output/phase08/meta_validation_report.json with structure:
```json
{
  "meta_totals": {
    "proportional": {"total": X, "non_zero": N, "formula_count": N},
    "equal": {"total": X, "non_zero": N, "formula_count": N},
    "dynamic": {"total": X, "non_zero": N, "formula_count": N}
  },
  "sap_reconciliation": {
    "projecao_total": X,
    "sap_total": 4747200,
    "delta": X,
    "delta_pct": X,
    "cause": "..."
  },
  "consultants": {
    "HEMANUELE DITZEL (MANU)": {"clients": N, "meta_anual": X, "real_anual": X, "pct": X},
    ...
    "OUTROS/SEM CONSULTOR": {"clients": N, "meta_anual": X, "real_anual": X, "pct": X}
  },
  "realizado_availability": {
    "JAN": {"col": "AA", "non_zero_count": N},
    ...
    "DEZ": {"col": "AL", "non_zero_count": N}
  },
  "indicators": {
    "pct_ytd": {"col": "AN", "formula_count": N},
    "sinal_meta": {"col": "AO", "formula_count": N},
    "gap": {"col": "AP", "formula_count": N},
    "ranking": {"col": "AQ", "formula_count": N}
  },
  "integrity": {
    "total_formulas": N,
    "min_required": 19224,
    "status": "PASS/FAIL"
  },
  "manu_alias": {
    "HEMANUELE_count": N,
    "MANU_DITZEL_count": N,
    "recommendation": "COMITE formulas must sum both names"
  },
  "overall_status": "PASS/PASS_WITH_NOTES"
}
```

9. **Print summary** to stdout with check() pattern (PASS/FAIL per check, like Phase 5/7 validation scripts).

IMPORTANT: This script is READ-ONLY. Do NOT modify V13. The only output is the JSON report + stdout.
  </action>
  <verify>
Run the script:
```bash
/c/Users/User/.pyenv/pyenv-win/pyenv-win/versions/3.12.10/python.exe scripts/phase08_comite_metas/01_validate_adjust_metas.py
```
Verify:
- Script exits 0
- data/output/phase08/meta_validation_report.json exists and is valid JSON
- Report shows >= 19,224 formulas
- Report shows ~R$ 4,779,003 proportional total
- Report shows SAP delta ~R$ 31,803 (0.67%)
- Report shows REALIZADO only in OCT/NOV/DEC months
- All 4 indicator columns have formula counts > 500
  </verify>
  <done>
meta_validation_report.json generated with complete audit: 3 meta column sets verified, SAP delta documented at 0.67%, consultant breakdown with MANU alias flagged, REALIZADO month availability mapped, 4 indicator columns confirmed as formulas, V13 integrity >= 19,224 formulas. All checks PASS or PASS_WITH_NOTES.
  </done>
</task>

</tasks>

<verification>
1. meta_validation_report.json exists with all required sections
2. Proportional meta total within 1% of R$ 4,779,003
3. SAP delta documented (R$ 31,803, 0.67%)
4. At least 4 consultants identified with client counts
5. MANU alias (both names) documented with recommendation
6. REALIZADO shows non-zero only for OCT/NOV/DEC 2025
7. All 4 indicator columns (AN, AO, AP, AQ) have formula counts > 500
8. V13 formula count >= 19,224
</verification>

<success_criteria>
- Script runs to completion without modifying V13
- JSON report is complete and accurate
- All meta infrastructure confirmed ready for COMITE consumption
- Consultant mapping provides the exact data contract Plan 08-02 needs
- No data gaps that would block COMITE construction
</success_criteria>

<output>
After completion, create `.planning/phases/08-comite-metas/08-01-SUMMARY.md`
</output>
