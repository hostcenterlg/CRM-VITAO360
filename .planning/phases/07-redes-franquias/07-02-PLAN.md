---
phase: 07-redes-franquias
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - scripts/phase07_redes_franquias/02_create_redes_tab.py
  - data/output/CRM_VITAO360_V13_PROJECAO.xlsx
autonomous: true

must_haves:
  truths:
    - "Aba REDES_FRANQUIAS_v2 existe no V13 com 19 redes + SEM GRUPO listadas"
    - "Formulas dinamicas SUMIFS/COUNTIFS referenciam PROJECAO corretamente (nao valores hardcoded)"
    - "Sinaleiro de penetracao calculado: SINALEIRO% = FAT.REAL / FAT.POTENCIAL com faixas de cor"
    - "Metas 6M (JAN-JUN 2026) populadas a partir dos dados SAP para cada rede"
    - "GAP = META_6M - FAT.REAL calculado e exibido por rede"
    - "Zero #REF! na aba REDES_FRANQUIAS_v2"
    - "19.224 formulas PROJECAO preservadas intactas"
  artifacts:
    - path: "scripts/phase07_redes_franquias/02_create_redes_tab.py"
      provides: "Script para criar aba REDES_FRANQUIAS_v2 com formulas dinamicas"
      min_lines: 250
    - path: "data/output/CRM_VITAO360_V13_PROJECAO.xlsx"
      provides: "V13 com nova aba REDES_FRANQUIAS_v2 completa"
      contains: "REDES_FRANQUIAS_v2"
  key_links:
    - from: "REDES_FRANQUIAS_v2 (col E: FAT.REAL)"
      to: "PROJECAO col Z"
      via: "SUMIFS formula"
      pattern: "SUMIFS.*PROJECAO.*Z.*C"
    - from: "REDES_FRANQUIAS_v2 (col D: CLIENTES ATIVOS)"
      to: "PROJECAO col C"
      via: "COUNTIF formula"
      pattern: "COUNTIF.*PROJECAO.*C"
    - from: "scripts/phase07_redes_franquias/02_create_redes_tab.py"
      to: "data/sources/sap/BASE_SAP_META_PROJECAO_2026.xlsx"
      via: "Leitura aba Faturamento para metas 6M por rede"
      pattern: "BASE_SAP_META.*Faturamento"
---

<objective>
Criar a aba REDES_FRANQUIAS_v2 no V13 com estrutura completa de sinaleiro de penetracao: 20 colunas (A:T), formulas dinamicas SUMIFS/COUNTIFS referenciando PROJECAO, metas 6M do SAP, calculo de GAP, e faixas de cor por penetracao. A aba deve funcionar sem #REF! e atualizar automaticamente quando dados da PROJECAO mudam.

Purpose: Entregar a visao consolidada por rede que permite ao gestor identificar oportunidades de crescimento (redes com baixa penetracao = potencial nao explorado) e priorizar acoes comerciais. Esta aba substitui/evolui o SINALEIRO_REDES do V11.
Output: V13 com aba REDES_FRANQUIAS_v2 funcional, zero #REF!, formulas dinamicas, metas 6M operacionais.
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-redes-franquias/07-RESEARCH.md
@.planning/phases/07-redes-franquias/07-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar aba REDES_FRANQUIAS_v2 com formulas dinamicas e metas 6M</name>
  <files>scripts/phase07_redes_franquias/02_create_redes_tab.py, data/output/CRM_VITAO360_V13_PROJECAO.xlsx</files>
  <action>
Criar `scripts/phase07_redes_franquias/02_create_redes_tab.py` que:

1. **Extrair metas 6M do SAP:**
   - Ler `data/sources/sap/BASE_SAP_META_PROJECAO_2026.xlsx`, aba "Faturamento"
   - Filtrar linhas onde GRUPO PRODUTO = "01. TOTAL" (meta total por rede, nao por produto)
   - Para cada rede/grupo chave: somar colunas JAN + FEV + MAR + ABR + MAI + JUN (6 meses)
   - Normalizar nome da rede usando REDE_NORMALIZE (mesmo mapa do 07-01)
   - Armazenar rede_to_meta6m dict
   - Valores esperados de referencia (da pesquisa):
     * FITLAND: R$ 283.500
     * CIA DA SAUDE: R$ 351.000
     * VIDA LEVE: R$ 154.500
     * DIVINA TERRA: R$ 157.500
     * BIO MUNDO: R$ 42.000
     * MUNDO VERDE: R$ 21.000
     * TUDO EM GRAOS: R$ 31.500
     * NATURVIDA: R$ 18.000
     * TRIP: R$ 9.000
     * LIGEIRINHO: R$ 9.000

2. **Extrair TOTAL LOJAS do SAP Leads:**
   - Ler `data/sources/sap/BASE_SAP_META_PROJECAO_2026.xlsx`, aba "Leads"
   - Para cada rede: total_lojas = coluna Total
   - Normalizar nomes
   - Armazenar rede_to_lojas dict

3. **Abrir V13** com `data_only=False`
   - Encontrar aba PROJECAO com accent-stripping
   - Determinar nome exato da aba PROJECAO (com acentos, espaco no final, etc.) para usar nas formulas
   - CRITICO: O nome da aba nas formulas DEVE ser exatamente como aparece no workbook (ex: "'PROJECAO '!" com espaco e aspas se necessario)

4. **Remover aba REDES_FRANQUIAS_v2 se ja existir** (idempotencia):
   - `if 'REDES_FRANQUIAS_v2' in wb.sheetnames: del wb['REDES_FRANQUIAS_v2']`

5. **Criar aba REDES_FRANQUIAS_v2:**
   ```
   ws = wb.create_sheet('REDES_FRANQUIAS_v2')
   ```

6. **Escrever titulo e subtitulo:**
   - A1: "SINALEIRO DE PENETRACAO -- REDES E FRANQUIAS VITAO (v2)"
   - A2: "Dados dinamicos | Atualiza automaticamente da aba PROJECAO | Benchmark: R$525/mes/loja | 11 meses"
   - Merge A1:T1 e A2:T2 para titulo largo

7. **Escrever PARAMETROS (area de referencia):**
   - V3: "PARAMETROS"
   - V4: "Benchmark (R$/mes/loja):", W4: 525
   - V5: "Meses dados:", W5: 11
   - V6: "Faixas:"
   - V7: "ROXO", W7: "0-1%", X7: "PROSPECCAO"
   - V8: "VERMELHO", W8: "1-40%", X8: "ATIVACAO/POSITIVACAO"
   - V9: "AMARELO", W9: "40-60%", X9: "SELL OUT"
   - V10: "VERDE", W10: "60-100%", X10: "JBP"
   - Isso permite ao usuario ajustar benchmark sem editar formulas

8. **Escrever headers (row 3, cols A:T = 20 colunas):**
   Headers: REDE, CONSULTOR, TOTAL LOJAS (SAP), ATIVOS, INAT.REC, INAT.ANT, PROSPECT, C/VENDA, N.PEDIDOS, FAT.REAL (R$), TICKET MEDIO (R$), FAT.POTENCIAL (R$), META 6M (R$), SINALEIRO %, COR, MATURIDADE, GAP (R$), PENETRACAO %, ACAO RECOMENDADA, PRIORIDADE

9. **Para cada rede (19 redes + SEM GRUPO = 20 linhas, rows 4-23):**
   Construir lista de redes ordenada por meta 6M descendente.
   Para cada rede na posicao `i` (row = 4+i):

   Usar nome exato da aba PROJECAO para formulas. Se aba se chama `PROJECAO ` (com espaco), formulas devem usar `'PROJECAO '!`.
   Abreviar: `PRJ` = nome da aba PROJECAO para referencia.

   * **Col A (REDE):** Nome da rede (string estatica, ex: "FITLAND")
   * **Col B (CONSULTOR):** Deixar vazio (sera preenchido manualmente ou em fase futura)
   * **Col C (TOTAL LOJAS):** Valor estatico do SAP Leads (rede_to_lojas[rede] ou 0)
   * **Col D (ATIVOS):** Formula COUNTIFS que conta clientes ativos da rede
     `=COUNTIFS('{PRJ}'!C$4:C$537,A{row},'{PRJ}'!Z$4:Z$537,">"&0)`
     (Conta clientes dessa rede com REALIZADO > 0)
   * **Col E (INAT.REC):** Deixar 0 (requer dados de ciclo de compra, sera populado em Phase 9)
   * **Col F (INAT.ANT):** Deixar 0 (idem)
   * **Col G (PROSPECT):** Formula:
     `=C{row}-D{row}-E{row}-F{row}`
     (Total lojas - ativos - inat.rec - inat.ant = prospects)
   * **Col H (C/VENDA):** Formula COUNTIFS:
     `=COUNTIFS('{PRJ}'!C$4:C$537,A{row},'{PRJ}'!Z$4:Z$537,">"&0)`
     (Mesma que ATIVOS por enquanto -- clientes com venda)
   * **Col I (N.PEDIDOS):** Deixar 0 (requer dados de pedidos, Phase 9)
   * **Col J (FAT.REAL):** Formula SUMIFS:
     `=SUMIFS('{PRJ}'!Z$4:Z$537,'{PRJ}'!C$4:C$537,A{row})`
     (Soma REALIZADO de todos os clientes dessa rede)
   * **Col K (TICKET MEDIO):** Formula:
     `=IFERROR(J{row}/H{row},0)`
     (Fat.Real / Clientes com venda)
   * **Col L (FAT.POTENCIAL):** Formula:
     `=C{row}*$W$4*$W$5`
     (Total Lojas * Benchmark * Meses -- referencia os PARAMETROS)
   * **Col M (META 6M):** Valor estatico do SAP (rede_to_meta6m[rede] ou 0)
   * **Col N (SINALEIRO %):** Formula:
     `=IFERROR(J{row}/L{row},0)`
     (Fat.Real / Fat.Potencial)
   * **Col O (COR):** Formula IF aninhada:
     `=IF(N{row}<0.01,"ROXO",IF(N{row}<0.4,"VERMELHO",IF(N{row}<0.6,"AMARELO","VERDE")))`
   * **Col P (MATURIDADE):** Formula IF:
     `=IF(O{row}="ROXO","PROSPECCAO",IF(O{row}="VERMELHO","ATIVACAO/POSITIVACAO",IF(O{row}="AMARELO","SELL OUT","JBP")))`
   * **Col Q (GAP):** Formula:
     `=MAX(M{row}-J{row},0)`
     (Meta 6M - Fat.Real, minimo 0)
   * **Col R (PENETRACAO %):** Formula:
     `=IFERROR(D{row}/C{row},0)`
     (Ativos / Total Lojas)
   * **Col S (ACAO RECOMENDADA):** Formula IF:
     `=IF(O{row}="ROXO","Cadastrar e ativar lojas",IF(O{row}="VERMELHO","Aumentar frequencia de compra",IF(O{row}="AMARELO","Expandir mix de produtos","Manter e fidelizar")))`
   * **Col T (PRIORIDADE):** Formula:
     `=RANK(Q{row},$Q$4:$Q$23,0)`
     (Ranking por GAP descendente -- maior gap = maior prioridade)

10. **Adicionar linha TOTAL (row 24):**
    * A24: "TOTAL"
    * C24: `=SUM(C4:C23)` (total lojas)
    * D24: `=SUM(D4:D23)` (total ativos)
    * J24: `=SUM(J4:J23)` (fat.real total)
    * L24: `=SUM(L4:L23)` (fat.potencial total)
    * M24: `=SUM(M4:M23)` (meta 6M total)
    * N24: `=IFERROR(J24/L24,0)` (sinaleiro % global)
    * Q24: `=SUM(Q4:Q23)` (gap total)

11. **Aplicar formatacao basica (openpyxl styles):**
    - Headers (row 3): bold, background cinza claro
    - Titulo (row 1): bold, font size 14
    - Colunas monetarias (J, K, L, M, Q): number_format = '#,##0.00'
    - Colunas percentuais (N, R): number_format = '0.00%'
    - Largura de colunas: A=20, B=15, C-I=12, J-M=15, N-R=12, S=30, T=10
    - Linha TOTAL: bold
    - Nao aplicar conditional formatting (complexo demais via openpyxl, usuario faz no Excel)

12. **Salvar V13** (preservando todas as formulas existentes)

13. **Validacao pos-save:**
    - Reabrir V13 com data_only=False
    - Verificar aba REDES_FRANQUIAS_v2 existe
    - Contar celulas com formulas na aba (esperado: ~200+)
    - Verificar que NENHUMA formula contem #REF! (buscar "#REF" em todas as celulas)
    - Contar formulas PROJECAO (deve ser >= 19.224)
    - Verificar que todas as 20 redes tem dados em col A
    - Verificar que formulas SUMIFS/COUNTIFS referenciam aba PROJECAO corretamente

14. **Print relatorio de execucao:**
    - Aba REDES_FRANQUIAS_v2 criada com N formulas
    - Metas 6M por rede (valores estaticos carregados)
    - Total Lojas por rede (valores estaticos carregados)
    - Distribuicao de redes por COR esperada
    - Zero #REF! confirmado
    - Formulas PROJECAO preservadas

**CRITICO -- Evitar #REF!:**
- Formulas DEVEM referenciar nome exato da aba PROJECAO (com acento, espaco, etc.)
- Se aba se chama `PROJECAO ` (com espaco no final), usar `'PROJECAO '!` nas formulas (com aspas simples!)
- Testar com uma formula antes de gerar todas: verificar que openpyxl nao rejeita a formula
- O range C$4:C$537 e Z$4:Z$537 DEVEM corresponder exatamente as 534 rows de dados

**NAO FAZER:**
- NAO usar data_only=True ao salvar
- NAO hardcodar valores de FAT.REAL (usar SUMIFS)
- NAO hardcodar valores de ATIVOS (usar COUNTIFS)
- NAO criar formulas que referenciam abas inexistentes
- NAO usar conditional formatting via openpyxl (instavel)
  </action>
  <verify>
Rodar o script:
```bash
/c/Users/User/.pyenv/pyenv-win/pyenv-win/versions/3.12.10/python.exe scripts/phase07_redes_franquias/02_create_redes_tab.py
```

Verificar:
1. Script roda sem erro
2. Aba REDES_FRANQUIAS_v2 existe no V13

Validacao detalhada:
```bash
/c/Users/User/.pyenv/pyenv-win/pyenv-win/versions/3.12.10/python.exe -c "
import openpyxl
wb = openpyxl.load_workbook('data/output/CRM_VITAO360_V13_PROJECAO.xlsx', data_only=False)
print('Abas:', wb.sheetnames)
assert 'REDES_FRANQUIAS_v2' in wb.sheetnames, 'Aba nao encontrada!'
ws = wb['REDES_FRANQUIAS_v2']
# Check headers
headers = [ws.cell(row=3, column=c).value for c in range(1, 21)]
print('Headers:', headers)
# Check redes
redes = []
for r in range(4, 25):
    rede = ws.cell(row=r, column=1).value
    if rede:
        redes.append(rede)
print(f'Redes: {len(redes)} ({redes})')
# Check formulas (no hardcoded values)
fat_real_f = ws.cell(row=4, column=10).value  # Col J
sinaleiro_f = ws.cell(row=4, column=14).value  # Col N
print(f'FAT.REAL formula (J4): {fat_real_f}')
print(f'SINALEIRO formula (N4): {sinaleiro_f}')
# Check for #REF!
ref_errors = 0
for row in ws.iter_rows(min_row=3, max_row=25, max_col=20):
    for cell in row:
        if isinstance(cell.value, str) and '#REF' in cell.value:
            ref_errors += 1
            print(f'#REF! at {cell.coordinate}: {cell.value}')
print(f'Total #REF! errors: {ref_errors}')
# Check META 6M values
print('Meta 6M por rede:')
for r in range(4, 24):
    rede = ws.cell(row=r, column=1).value
    meta = ws.cell(row=r, column=13).value  # Col M
    if rede:
        print(f'  {rede}: {meta}')
# Count PROJECAO formulas
import unicodedata
for name in wb.sheetnames:
    clean = unicodedata.normalize('NFD', name)
    clean = ''.join(c for c in clean if unicodedata.category(c) != 'Mn')
    if 'PROJECAO' in clean.upper():
        ws_p = wb[name]
        formulas = sum(1 for row in ws_p.iter_rows(min_row=4, max_row=537) for cell in row if isinstance(cell.value, str) and cell.value.startswith('='))
        print(f'Formulas PROJECAO: {formulas}')
        break
wb.close()
"
```

Verificar:
1. 20 redes listadas (19 + SEM GRUPO)
2. FAT.REAL (J4) contem SUMIFS formula (nao valor estatico)
3. SINALEIRO (N4) contem IFERROR formula
4. Zero #REF! errors
5. Metas 6M preenchidas para redes principais (FITLAND ~283500, CIA DA SAUDE ~351000)
6. Formulas PROJECAO >= 19.224
  </verify>
  <done>
- Aba REDES_FRANQUIAS_v2 criada no V13 com 20 colunas (A:T) e 20 redes (19 + SEM GRUPO)
- Formulas dinamicas: FAT.REAL via SUMIFS, ATIVOS via COUNTIFS, SINALEIRO% calculado, COR/MATURIDADE derivados
- Metas 6M populadas com dados SAP para todas as redes com meta > 0
- GAP = META_6M - FAT.REAL calculado por rede
- Ranking de prioridade por GAP
- PARAMETROS editaveis (benchmark, meses, faixas de cor)
- Zero #REF! na aba
- 19.224+ formulas PROJECAO preservadas
  </done>
</task>

</tasks>

<verification>
1. Aba REDES_FRANQUIAS_v2 existe no V13
2. 20 linhas de dados (19 redes + SEM GRUPO) + 1 linha TOTAL
3. Formulas SUMIFS/COUNTIFS referenciam PROJECAO corretamente
4. Zero #REF! na aba inteira
5. Metas 6M preenchidas com valores do SAP
6. PARAMETROS area com benchmark R$525 editavel
7. Formulas PROJECAO intactas (>= 19.224)
8. Nenhuma outra aba afetada
</verification>

<success_criteria>
- REDES_FRANQUIAS_v2 funcional com formulas dinamicas (nao valores hardcoded)
- Zero #REF! garantido (REDE-02 cumprido)
- Sinaleiro de penetracao com faixas de cor corretas (REDE-03 parcial)
- Metas 6M operacionais por rede (REDE-04 cumprido)
- PROJECAO formulas preservadas
</success_criteria>

<output>
After completion, create `.planning/phases/07-redes-franquias/07-02-SUMMARY.md`
</output>
