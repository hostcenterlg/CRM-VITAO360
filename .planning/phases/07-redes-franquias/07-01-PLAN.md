---
phase: 07-redes-franquias
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/phase07_redes_franquias/01_remap_expand_reftable.py
  - data/output/CRM_VITAO360_V13_PROJECAO.xlsx
autonomous: true

must_haves:
  truths:
    - "11 clientes SEM GRUPO remapeados para rede correta via CNPJ match com SAP (7 ESMERALDA, 1 DIVINA TERRA, 1 MERCOCENTRO, 1 MINHA QUITANDINHA, 1 VIDA LEVE)"
    - "Tabela de referencia AS:AZ expandida de 12 para 19 redes com dados 2025 reais"
    - "VLOOKUPs nas colunas F:J atualizados para referenciar range expandido (AS$4:AX$22 ao inves de AS$4:AX$18)"
    - "19.224 formulas PROJECAO preservadas intactas apos todas as modificacoes"
    - "Cada rede na tabela AS:AZ tem TOTAL LOJAS (SAP Leads), FAT.REAL (SUMIFS do PROJECAO col Z), SINALEIRO%, COR, MATURIDADE, ACAO, GAP"
  artifacts:
    - path: "scripts/phase07_redes_franquias/01_remap_expand_reftable.py"
      provides: "Script para remapear clientes e expandir tabela de referencia"
      min_lines: 200
    - path: "data/output/CRM_VITAO360_V13_PROJECAO.xlsx"
      provides: "V13 atualizado com redes corrigidas e tabela expandida"
      contains: "PROJECAO"
  key_links:
    - from: "scripts/phase07_redes_franquias/01_remap_expand_reftable.py"
      to: "data/sources/sap/01_SAP_CONSOLIDADO.xlsx"
      via: "Leitura col AQ (06 Nome Grupo Chave) para CNPJ->rede mapping"
      pattern: "Cadastro Clientes SAP.*col.*AQ"
    - from: "scripts/phase07_redes_franquias/01_remap_expand_reftable.py"
      to: "data/output/CRM_VITAO360_V13_PROJECAO.xlsx"
      via: "openpyxl data_only=False para preservar formulas"
      pattern: "load_workbook.*data_only.*False"
    - from: "scripts/phase07_redes_franquias/01_remap_expand_reftable.py"
      to: "data/sources/sap/BASE_SAP_META_PROJECAO_2026.xlsx"
      via: "Leitura aba Leads para TOTAL LOJAS por rede"
      pattern: "BASE_SAP_META.*Leads"
---

<objective>
Remapear 11 clientes SEM GRUPO para a rede correta via CNPJ match com SAP Cadastro, expandir a tabela de referencia AS:AZ de 12 para 19 redes com dados 2025 reais (Total Lojas do SAP Leads, Fat.Real calculado do PROJECAO), e atualizar os VLOOKUPs das colunas F:J para referenciar o range expandido.

Purpose: Corrigir os 11 clientes mal-classificados como SEM GRUPO e preparar a infraestrutura de dados de rede (tabela AS:AZ expandida) que sera consumida tanto pelos VLOOKUPs do PROJECAO quanto pela nova aba REDES_FRANQUIAS_v2 no plan 07-02.
Output: V13 com col C corrigida para 11 clientes, tabela AS:AZ com 19 redes e dados atualizados, VLOOKUPs apontando para range expandido.
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-redes-franquias/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remapear 11 clientes SEM GRUPO e expandir tabela de referencia AS:AZ</name>
  <files>scripts/phase07_redes_franquias/01_remap_expand_reftable.py, data/output/CRM_VITAO360_V13_PROJECAO.xlsx</files>
  <action>
Criar `scripts/phase07_redes_franquias/01_remap_expand_reftable.py` que:

1. **Construir mapa SAP: CNPJ -> Grupo Chave normalizado:**
   - Ler `data/sources/sap/01_SAP_CONSOLIDADO.xlsx`, aba "Cadastro Clientes SAP"
   - Col E (ou col com CNPJ) = CNPJ, Col AQ = "06 Nome Grupo Chave"
   - Para cada cliente com grupo != "06 - SEM GRUPO" e != vazio:
     * Normalizar CNPJ: remover `.`, `-`, `/`, zfill(14)
     * Normalizar nome da rede: usar mapa REDE_NORMALIZE (ver 07-RESEARCH.md Pattern 2)
       - Ex: "06 - INTERNA - BIO MUNDO" -> "BIO MUNDO"
       - Ex: "06 - INTERNA - TUDO EM GRAOS / VGA" -> "TUDO EM GRAOS"
       - Ex: "06 - INTERNA - MINHA QUITANDINHA - SP" -> "MINHA QUITANDINHA"
     * Armazenar cnpj_to_rede[cnpj] = rede_normalizada
   - Contar e listar redes unicas encontradas no SAP (esperado: ~19)

2. **Remapear clientes SEM GRUPO no V13:**
   - Abrir V13 (`data/output/CRM_VITAO360_V13_PROJECAO.xlsx`) com `data_only=False`
   - Encontrar aba PROJECAO com accent-stripping (buscar aba cujo nome sem acentos contem "PROJECAO")
   - Para cada row 4-537 (534 clientes):
     * Ler CNPJ da col A (ou col 1), normalizar
     * Ler rede atual da col C (col 3)
     * Se rede == "SEM GRUPO" e cnpj esta no cnpj_to_rede:
       - Atualizar col C para cnpj_to_rede[cnpj]
       - Registrar: row, cnpj, rede_antiga, rede_nova
   - Esperado: 11 remapeamentos (7 ESMERALDA, 1 DIVINA TERRA, 1 MERCOCENTRO, 1 MINHA QUITANDINHA, 1 VIDA LEVE)
   - Se != 11, logar warning mas continuar (pode haver mais ou menos no V13 atual)

3. **Coletar dados reais 2025 para cada rede (apos remapeamento):**
   a. **TOTAL LOJAS** do SAP Leads:
      - Ler `data/sources/sap/BASE_SAP_META_PROJECAO_2026.xlsx`, aba "Leads"
      - Procurar coluna com nomes de rede e coluna "Total" ou ultima coluna numerica
      - Para cada rede: total_lojas = valor da coluna Total
      - Normalizar nomes usando REDE_NORMALIZE
      - Se rede nao encontrada no Leads, usar 0 e logar warning
   b. **FAT.REAL** do PROJECAO:
      - Para cada rede, somar col Z (REALIZADO) de todos os clientes dessa rede no PROJECAO
      - Usar ws_projecao para ler col Z (rows 4-537), agrupar por col C (REDE/GRUPO CHAVE)
      - Ler com data_only=True em copia separada (para obter valores, nao formulas)
      - CUIDADO: Abrir uma SEGUNDA instancia com data_only=True apenas para LEITURA dos valores calculados. Se col Z tiver formulas e data_only=True retornar None (arquivo nao foi aberto no Excel), usar abordagem alternativa: somar cols AA:AL (meses individuais, que sao valores numericos)
   c. **SINALEIRO%** = FAT.REAL / FAT.POTENCIAL
      - FAT.POTENCIAL = TOTAL_LOJAS * 525 * 11 (benchmark R$525/mes/loja, 11 meses)
      - Se TOTAL_LOJAS == 0: SINALEIRO% = 0
   d. **COR** baseada nas faixas de PARAMETROS:
      - 0-1%: "ROXO" (texto + emoji opcional)
      - 1-40%: "VERMELHO"
      - 40-60%: "AMARELO"
      - 60-100%: "VERDE"
      - >100%: "VERDE" (cap at 100% display)
   e. **MATURIDADE** baseada na COR:
      - ROXO -> "PROSPECCAO"
      - VERMELHO -> "ATIVACAO/POSITIVACAO"
      - AMARELO -> "SELL OUT"
      - VERDE -> "JBP"
   f. **ACAO RECOMENDADA** baseada na MATURIDADE:
      - PROSPECCAO -> "Cadastrar e ativar lojas"
      - ATIVACAO/POSITIVACAO -> "Aumentar frequencia de compra"
      - SELL OUT -> "Expandir mix de produtos"
      - JBP -> "Manter e fidelizar"
   g. **GAP** = FAT.POTENCIAL - FAT.REAL (se negativo, usar 0)

4. **Expandir tabela de referencia AS:AZ:**
   - Lista completa de 19 redes (12 existentes + 7 novas):
     Existentes: FITLAND, VIDA LEVE, CIA DA SAUDE, DIVINA TERRA, BIO MUNDO, MUNDO VERDE, NATURVIDA, TUDO EM GRAOS, TRIP, MAIS NATURAL, LIGEIRINHO, PROSAUDE
     Novas: ARMAZEM FIT STORE, ESMERALDA, NOVA GERACAO, MERCOCENTRO, JARDIM DAS ERVAS, FEDERZONI, MIX VALI
   - Ordernar por FAT.REAL descendente (maiores redes primeiro)
   - Escrever nas colunas AS:AZ, rows 4 ate 4+18 (19 redes = rows 4-22):
     * AS (col 45) = REDE nome
     * AT (col 46) = TOTAL LOJAS (int)
     * AU (col 47) = SINALEIRO % (float, 2 casas decimais)
     * AV (col 48) = COR (texto: ROXO/VERMELHO/AMARELO/VERDE)
     * AW (col 49) = MATURIDADE (texto)
     * AX (col 50) = ACAO RECOMENDADA (texto)
     * AY (col 51) = FAT. REAL (float, 2 casas decimais)
     * AZ (col 52) = GAP (float, 2 casas decimais)
   - Limpar rows 23+ (caso existam dados antigos alem das 12 redes originais)

5. **Atualizar VLOOKUPs nas colunas F:J:**
   - As formulas atuais referenciam AS$4:AX$18 (12 redes, rows 4-15 + margem)
   - Atualizar para AS$4:AX$22 (19 redes, rows 4-22)
   - Para cada row 4-537 e cada col F:J (cols 6-10):
     * Ler formula atual
     * Se contem "AS$4:AX$18" (ou variante de range), substituir por "AS$4:AX$22"
     * Se contem outro range AS:AX, ajustar endpoint para row 22
   - Usar string replace simples: formula.replace("AX$18", "AX$22").replace("AX$15", "AX$22")
   - CUIDADO: Nao alterar formulas que nao contenham referencias AS:AX

6. **Adicionar entrada SEM GRUPO na tabela (row 23):**
   - AS = "SEM GRUPO"
   - AT:AZ = agregar dados dos 394 clientes SEM GRUPO (lojas, fat.real, etc.)
   - Isso permite que os VLOOKUPs dos clientes SEM GRUPO tambem retornem valores

7. **Salvar V13** (preservando todas as formulas)

8. **Validacao pos-save:**
   - Reabrir V13 com data_only=False
   - Contar formulas no PROJECAO (deve ser >= 19.224)
   - Verificar que 11 clientes foram remapeados (col C != SEM GRUPO para CNPJs esperados)
   - Verificar que tabela AS:AZ tem 20 rows de dados (19 redes + SEM GRUPO)
   - Verificar que VLOOKUPs em F:J referenciam range expandido
   - Verificar freeze_panes preservado

9. **Print relatorio de execucao:**
   - Remapeamentos realizados (cnpj, de, para)
   - Distribuicao atualizada de redes (contagem por rede apos remapeamento)
   - Tabela AS:AZ atualizada (19 redes + SEM GRUPO com dados)
   - Contagem de formulas preservadas
   - VLOOKUPs atualizados (contagem)

**Reutilizar padroes de Phases anteriores:**
- `normalize_cnpj()` pattern: remover `.`, `-`, `/`, zfill(14) -- copiar inline
- `PROJECT_ROOT` via `Path(__file__).resolve().parent.parent.parent`
- Accent-stripping para sheet name lookup
- `safe_float()` / `safe_int()` para converter valores

**NAO FAZER:**
- NAO usar data_only=True ao salvar (destroi formulas)
- NAO sobrescrever formulas em colunas F:J -- apenas atualizar o range reference
- NAO alterar rows/cols fora do escopo (manter DASH, LOG, CARTEIRA intactos)
- NAO ignorar as 7 redes extras do SAP -- incluir todas
  </action>
  <verify>
Rodar o script:
```bash
/c/Users/User/.pyenv/pyenv-win/pyenv-win/versions/3.12.10/python.exe scripts/phase07_redes_franquias/01_remap_expand_reftable.py
```

Verificar:
1. Script roda sem erro
2. 11 clientes remapeados (verificar output do script)
3. Tabela AS:AZ tem 19 redes + SEM GRUPO (20 rows de dados)
4. VLOOKUPs em F:J atualizados para range expandido

Validacao adicional:
```bash
/c/Users/User/.pyenv/pyenv-win/pyenv-win/versions/3.12.10/python.exe -c "
import openpyxl
wb = openpyxl.load_workbook('data/output/CRM_VITAO360_V13_PROJECAO.xlsx', data_only=False)
ws = None
for name in wb.sheetnames:
    import unicodedata
    clean = unicodedata.normalize('NFD', name)
    clean = ''.join(c for c in clean if unicodedata.category(c) != 'Mn')
    if 'PROJECAO' in clean.upper():
        ws = wb[name]
        break
# Count formulas
formulas = sum(1 for row in ws.iter_rows(min_row=4, max_row=537) for cell in row if isinstance(cell.value, str) and cell.value.startswith('='))
print(f'Formulas PROJECAO: {formulas}')
# Check redes in col C
from collections import Counter
redes = Counter()
for r in range(4, 538):
    v = ws.cell(row=r, column=3).value
    redes[v] = redes.get(v, 0) + 1
print(f'Distribuicao redes:')
for rede, cnt in sorted(redes.items(), key=lambda x: -x[1]):
    print(f'  {rede}: {cnt}')
# Check ref table AS:AZ
print(f'Tabela AS:AZ:')
for r in range(4, 25):
    rede = ws.cell(row=r, column=45).value
    if rede:
        lojas = ws.cell(row=r, column=46).value
        fat = ws.cell(row=r, column=51).value
        print(f'  Row {r}: {rede} | Lojas={lojas} | Fat={fat}')
# Check VLOOKUP range
f4 = ws.cell(row=4, column=6).value
print(f'VLOOKUP F4: {f4}')
wb.close()
"
```

Verificar:
1. Formulas PROJECAO >= 19.224
2. SEM GRUPO reduzido de 405 para 394
3. ESMERALDA com 7 clientes (antes 0)
4. Tabela AS:AZ com 19+ redes com dados
5. VLOOKUP F4 referencia AX$22 (nao AX$18)
  </verify>
  <done>
- 11 clientes remapeados de SEM GRUPO para rede correta (7 ESMERALDA, 1 DIVINA TERRA, 1 MERCOCENTRO, 1 MINHA QUITANDINHA, 1 VIDA LEVE)
- Tabela AS:AZ expandida para 19 redes + SEM GRUPO com dados 2025 reais (TOTAL LOJAS, FAT.REAL, SINALEIRO%, COR, MATURIDADE, ACAO, GAP)
- VLOOKUPs em F:J atualizados para range expandido AS$4:AX$22
- 19.224+ formulas PROJECAO preservadas intactas
- Script re-executavel e idempotente
  </done>
</task>

</tasks>

<verification>
1. V13 aberto sem erros, 19.224+ formulas intactas
2. Col C: 394 SEM GRUPO (antes 405), 140 com rede (antes 129)
3. Tabela AS:AZ: 19 redes + SEM GRUPO com TOTAL LOJAS e FAT.REAL preenchidos
4. VLOOKUPs F:J apontam para range AS$4:AX$22
5. freeze_panes preservado
6. Nenhuma outra aba (DASH, LOG, CARTEIRA) afetada
</verification>

<success_criteria>
- 11 clientes corretamente remapeados de SEM GRUPO para rede SAP
- Tabela de referencia expandida de 12 para 19 redes com dados reais 2025
- VLOOKUPs funcionais com range expandido
- Zero perda de formulas no PROJECAO
</success_criteria>

<output>
After completion, create `.planning/phases/07-redes-franquias/07-01-SUMMARY.md`
</output>
