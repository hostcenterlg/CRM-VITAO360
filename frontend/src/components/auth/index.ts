/**
 * CRM VITAO360 — Barrel export: componentes de autorizacao RBAC.
 *
 * Uso:
 *   import { RequireRole, RoleGuard, useRoleGuard } from '@/components/auth';
 */

export { RequireRole } from './RequireRole';
export { RoleGuard, useRoleGuard } from './RoleGuard';
